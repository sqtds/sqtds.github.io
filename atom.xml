<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title><![CDATA[蜻蜓点水]]></title>
  <subtitle><![CDATA[if you never try , you will lost forever.]]></subtitle>
  <link href="/atom.xml" rel="self"/>
  <link href="http://sqtds.github.io/"/>
  <updated>2015-05-17T07:52:13.000Z</updated>
  <id>http://sqtds.github.io/</id>
  
  <author>
    <name><![CDATA[sqtds]]></name>
    <email><![CDATA[858569159@qq.com]]></email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title><![CDATA[java核心系列]]></title>
    <link href="http://sqtds.github.io/2015/05/17/2015/java-core/"/>
    <id>http://sqtds.github.io/2015/05/17/2015/java-core/</id>
    <published>2015-05-17T07:13:23.000Z</published>
    <updated>2015-05-17T07:52:13.000Z</updated>
    <content type="html"><![CDATA[<p><img src="/img/2015/Java核心.png" alt="图1"></p>
<h2 id="一，概述">一，概述</h2><p>俗话说，只有地基打的好，楼层才能盖得高。对于程序员来说，也是一样。我们也需要不断反复的理解以前的东西，打好自己的基础，才能在技术上又更大的进步。</p>
<p>本系列主要介绍j2se的核心部分，希望对大家梳理自己的知识体系有所帮助。本系列并不完善，以后会逐步添加新的东西。</p>
<p>当然，本系列暂时不涉及计算机基础知识，这些知识也需要程序员去理解。</p>
<h2 id="二，目标">二，目标</h2><p>本系列主要从图1中绘制的几大主线来讲解java核心。<br>初步设计采用问答式和总结式来介绍各个组件的特性，以加深读者对java核心的理解。<br>本系列只是个引子，更深入的内容请参考文中的参考资料。</p>
<h2 id="三，总结">三，总结</h2><p>暂时没有了，就这么点，如果大家有好的意见可以提出来。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p><img src="/img/2015/Java核心.png" alt="图1"></p>
<h2 id="一，概述">一，概述</h2><p>俗话说，只有地基打的好，楼层才能盖得高。对于程序员来说，也是一样。我们也需要不断反复的理解以前的东西，打好自己的基础，才能在技术上]]>
    </summary>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[java对象大小]]></title>
    <link href="http://sqtds.github.io/2015/05/10/2015/java-object-size/"/>
    <id>http://sqtds.github.io/2015/05/10/2015/java-object-size/</id>
    <published>2015-05-10T12:57:27.000Z</published>
    <updated>2015-05-10T13:42:01.000Z</updated>
    <content type="html"><![CDATA[<h2 id="对象大小">对象大小</h2><p><img src="/img/2015/java_object_size.png" alt=""></p>
<h3 id="1，64位系统">1，64位系统</h3><p>new java.lang.Object() 占用了 16 bytes<br>new byte[0] 占用了 24 bytes</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line"> <span class="keyword">byte</span> x;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line"> <span class="keyword">byte</span> y;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>new A() 占用了 24 bytes<br>new B() 占用了 32 bytes= 24+pad A(8)</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> </span>&#123;</span><br><span class="line"> Object obj = <span class="keyword">new</span> Object();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>new C() takes 40 bytes= obj+obj ref +C = 16+8+16</p>
<h3 id="2，参考资料">2，参考资料</h3><ul>
<li><a href="http://www.importnew.com/14948.html" target="_blank" rel="external">一个Java对象到底占多大内存？</a></li>
<li><a href="http://www.infoq.com/presentations/JVM-Performance-Tuning-twitter" target="_blank" rel="external">everything-i-ever-learned-about-jvm-performance-tuning-twitter</a></li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="对象大小">对象大小</h2><p><img src="/img/2015/java_object_size.png" alt=""></p>
<h3 id="1，64位系统">1，64位系统</h3><p>new java.lang.Object() 占用了 1]]>
    </summary>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="memory" scheme="http://sqtds.github.io/tags/memory/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[java队列]]></title>
    <link href="http://sqtds.github.io/2015/05/10/2015/java-queue/"/>
    <id>http://sqtds.github.io/2015/05/10/2015/java-queue/</id>
    <published>2015-05-10T12:31:10.000Z</published>
    <updated>2015-05-17T05:23:18.000Z</updated>
    <content type="html"><![CDATA[<h2 id="队列概览">队列概览</h2><p><img src="/img/2015/Queue.png" alt=""></p>
<h3 id="1，BlockingQueue">1，BlockingQueue</h3><ul>
<li>ArrayBlockingQueue，数组结构组成,其构造函数必须带一个int参数来指明其大小</li>
<li>LinkedBlockingQueue，由链表结构组成,若其构造函数带一个规定大小的参数，生成的BlockingQueue有大小限制，若不带大小参数，所生成的BlockingQueue的大小由Integer.MAX_VALUE来决定</li>
<li>PriorityBlockingQueue，其所含对象的排序不是FIFO,而是依据对象的自然排序顺序或者是构造函数的Comparator决定的顺序</li>
<li><p>DelayQueue：一个使用优先级队列实现的无界阻塞队列。DelayQueue是一个支持延时获取元素的无界阻塞队列。队列使用PriorityQueue来实现。队列中的元素必须实现Delayed接口，<strong>在创建元素时可以指定多久才能从队列中获取当前元素</strong>。<strong>只有在延迟期满时才能从队列中提取元素。</strong>我们可以将DelayQueue运用在以下应用场景：</p>
<p> 缓存系统的设计：<br> 可以用DelayQueue保存缓存元素的有效期，使用一个线程循环查询DelayQueue，一旦能从DelayQueue中获取元素时，表示缓存有效期到了。<br> 定时任务调度：<br> 使用DelayQueue保存当天将会执行的任务和执行时间，一旦从DelayQueue中获取到任务就开始执行，从比如TimerQueue就是使用DelayQueue实现的。</p>
</li>
<li><p>SynchronousQueue：一个不存储元素的阻塞队列。每一个put操作必须等待一个take操作，否则不能继续添加元素。</p>
</li>
<li>LinkedTransferQueue：一个由链表结构组成的无界阻塞队列。</li>
<li>LinkedBlockingDeque：一个由链表结构组成的双向阻塞队列。</li>
</ul>
<h3 id="2，Queue">2，Queue</h3><ul>
<li><p>PriorityQueue是个基于优先级堆的极大优先级队列。<br>此队列按照在构造时所指定的顺序对元素排序，既可以根据元素的自然顺序来指定排序（参阅 Comparable）， 也可以根据 Comparator 来指定，这取决于使用哪种构造方法。优先级队列不允许 null 元素。</p>
</li>
<li><p>ConcurrentLinkedQueue一个无锁的并发线程安全的队列。对比锁机制的实现，使用无锁机制的难点在于要充分考虑线程间的协调。简单的说就是多个线程对内部数据结构进行访问时，如果其中一个线程执行的中途因为一些原因出现故障，其他的线程能够检测并帮助完成剩下的操作。这就需要把对数据结构的操作过程精细的划分成多个状态或阶段，考虑每个阶段或状态多线程访问会出现的情况。<br>需要说一下的是，<strong>ConcurrentLinkedQueue的size()是要遍历一遍集合的，所以尽量要避免用size而改用isEmpty()，以免性能过慢。</strong></p>
</li>
</ul>
<h3 id="3，Deque">3，Deque</h3><p>LinkedList，ArrayDeque，非线程安全的，可以通过Collections的工具方法改造成线程安全的。<br>LinkedBlockingDeque</p>
<p>按照我们一般的理解，Deque是一个双向队列，这将意味着它不过是对Queue接口的增强。如果仔细分析Deque接口代码的话，我们会发现它里面主要包含有4个部分的功能定义。</p>
<ol>
<li>双向队列特定方法定义。 </li>
<li>Queue方法定义。 </li>
<li><strong>Stack方法定义。 </strong></li>
<li>Collection方法定义。</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">\\Stack定义</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">push</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">        addFirst(e);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> E <span class="title">pop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> removeFirst();</span><br><span class="line">    &#125;</span><br><span class="line"> 	<span class="function"><span class="keyword">public</span> E <span class="title">peek</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<h3 id="4,参考资料">4,参考资料</h3><p><a href="http://www.infoq.com/cn/articles/java-blocking-queue" target="_blank" rel="external">聊聊并发（七）——Java中的阻塞队列</a><br><a href="http://blog.csdn.net/guangcigeyun/article/details/8278350" target="_blank" rel="external"> Java多线程（五）之BlockingQueue深入分析</a><br><a href="http://blog.csdn.net/guangcigeyun/article/details/8278355" target="_blank" rel="external">Java多线程（六）之Deque与LinkedBlockingDeque深入分析</a></p>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="队列概览">队列概览</h2><p><img src="/img/2015/Queue.png" alt=""></p>
<h3 id="1，BlockingQueue">1，BlockingQueue</h3><ul>
<li>ArrayBlockingQueu]]>
    </summary>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[java线程]]></title>
    <link href="http://sqtds.github.io/2015/05/10/2015/java-thread/"/>
    <id>http://sqtds.github.io/2015/05/10/2015/java-thread/</id>
    <published>2015-05-10T10:01:58.000Z</published>
    <updated>2015-05-10T13:01:03.000Z</updated>
    <content type="html"><![CDATA[<h2 id="thread">thread</h2><p><img src="/img/2015/thread.png" alt=""></p>
<h3 id="1，锁">1，锁</h3><p>偏向锁(Biased Locking)是Java6引入的一项多线程优化。它通过消除资源无竞争情况下的同步原语，进一步提高了程序的运行性能。</p>
<p>轻量级锁（Lightweight Locking）本意是为了减少多线程进入互斥的几率，并不是要替代互斥。</p>
<p>轻量级锁也是一种多线程优化，它与偏向锁的区别在于，轻量级锁是通过CAS来避免进入开销较大的互斥操作，而偏向锁是在无竞争场景下完全消除同步，连CAS也不执行（CAS本身仍旧是一种操作系统同步原语，始终要在JVM与OS之间来回，有一定的开销）。</p>
<h3 id="2，同步">2，同步</h3><p>wait:方法使当前线程暂停执行并释放对象锁标示，让其他线程可以进入synchronized数据块，当前线程被放入对象等待池中。</p>
<p>notify:将从对象的等待池中移走一个任意的线程并放到锁标志等待池中，只有锁标志等待池中线程能够获取锁标志；如果锁标志等待池中没有线程，则notify()不起作用。</p>
<p>notifyAll:从对象等待池中移走所有等待那个对象的线程并放到锁标志等待池中。</p>
<p><strong>注意 这三个方法都是java.lang.Object的方法,调用时需要获取锁</strong></p>
<h3 id="3，实例方法">3，实例方法</h3><p>join:主要是让调用改方法的thread完成run方法里面的东西后， 在执行join()方法后面的代码。</p>
<pre><code class="java">t1.start();  
t1.join(); <span class="comment">// wait t1 to be finished  </span>
t2.start();  
t2.join(); <span class="comment">// in this program, this may be removed</span>
</code></pre>
<p>interupt:中断是一种协作机制。当一个线程中断另一个线程时，被中断的线程不一定要立即停止正在做的事情。相反，中断是礼貌地请求另一个线程在它愿意并且方便的时候停止它正在做的事情。</p>
<h3 id="4，静态方法">4，静态方法</h3><p>sleep：在指定的毫秒数内让当前正在执行的线程休眠（暂停执行），此操作受到系统计时器和调度程序精度和准确性的影响。该线程不丢失任何监视器的所属权。 通过调用sleep使任务进入休眠状态，在这种情况下，任务在指定的时间内不会运行。调用sleep的时候锁并没有被释放。</p>
<p>yield：线程调用yield()方法后，表明自己做的事已经完成，让出自己的cpu时间给其他线程使用。让出后，该线程可以重新获得cpu分配的权利，状态变为了可执行状态。（yield并不意味着退出和暂停，只是，告诉线程调度如果有人需要，可以先拿去，我过会再执行，没人需要，我继续执行）调用yield的时候锁并没有被释放。</p>
<h3 id="5，参考资料">5，参考资料</h3><ul>
<li><a href="http://blog.csdn.net/hsuxu/article/details/9472371" target="_blank" rel="external">java中的Synchronized 实现</a></li>
<li><a href="http://www.ibm.com/developerworks/cn/java/j-jtp05236.html" target="_blank" rel="external">Java 理论与实践: 处理 InterruptedException</a></li>
<li><a href="http://ifeve.com/java-concurrency-constructs/" target="_blank" rel="external">Java并发结构</a></li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="thread">thread</h2><p><img src="/img/2015/thread.png" alt=""></p>
<h3 id="1，锁">1，锁</h3><p>偏向锁(Biased Locking)是Java6引入的一项多线程优化。它通过消除资]]>
    </summary>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="thread" scheme="http://sqtds.github.io/tags/thread/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[java并发编程]]></title>
    <link href="http://sqtds.github.io/2015/05/10/2015/%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/"/>
    <id>http://sqtds.github.io/2015/05/10/2015/并发编程/</id>
    <published>2015-05-10T08:13:15.000Z</published>
    <updated>2015-05-10T10:15:28.000Z</updated>
    <content type="html"><![CDATA[<h2 id="并发编程概览">并发编程概览</h2><p><img src="/img/2015/并发编程.png" alt="并发编程"></p>
<h3 id="1,容器">1,容器</h3><p>略</p>
<h3 id="2，协作">2，协作</h3><p>略</p>
<h3 id="3，变量同步">3，变量同步</h3><p>略</p>
<h3 id="4，内存模型">4，内存模型</h3><p>略</p>
<h3 id="5，参考资料">5，参考资料</h3><ul>
<li>《java 并发编程实践》</li>
<li><a href="http://www.infoq.com/cn/articles/java-memory-model-1?utm_source=infoq&amp;utm_medium=related_content_link&amp;utm_campaign=relatedContent_articles_clk" target="_blank" rel="external">深入理解Java内存模型（一）——基础</a></li>
<li><a href="http://www.infoq.com/cn/articles/java-memory-model-2?utm_source=infoq&amp;utm_medium=related_content_link&amp;utm_campaign=relatedContent_articles_clk" target="_blank" rel="external">深入理解Java内存模型（二）——重排序</a></li>
<li><a href="http://www.infoq.com/cn/articles/java-memory-model-3?utm_source=infoq&amp;utm_medium=related_content_link&amp;utm_campaign=relatedContent_articles_clk" target="_blank" rel="external">深入理解Java内存模型（三）——顺序一致性</a></li>
<li><a href="http://www.infoq.com/cn/articles/ftf-java-volatile?utm_source=infoq&amp;utm_medium=related_content_link&amp;utm_campaign=relatedContent_articles_clk" target="_blank" rel="external">聊聊并发（一）——深入分析Volatile的实现原理</a></li>
<li><a href="http://www.infoq.com/cn/articles/java-se-16-synchronized" target="_blank" rel="external">聊聊并发（二）——Java SE1.6中的Synchronized</a></li>
<li><a href="http://www.infoq.com/cn/articles/java-threadPool" target="_blank" rel="external">聊聊并发（三）——JAVA线程池的分析和使用</a></li>
<li><a href="http://www.infoq.com/cn/articles/ConcurrentHashMap?utm_source=infoq&amp;utm_medium=related_content_link&amp;utm_campaign=relatedContent_articles_clk" target="_blank" rel="external">聊聊并发（四）——深入分析ConcurrentHashMap</a></li>
<li><a href="http://www.infoq.com/cn/articles/atomic-operation?utm_source=infoq&amp;utm_medium=related_content_link&amp;utm_campaign=relatedContent_articles_clk" target="_blank" rel="external">聊聊并发（五）——原子操作的实现原理</a></li>
<li><a href="http://www.infoq.com/cn/articles/ConcurrentLinkedQueue?utm_source=infoq&amp;utm_medium=related_content_link&amp;utm_campaign=relatedContent_articles_clk" target="_blank" rel="external">聊聊并发（六）——ConcurrentLinkedQueue的实现原理分析</a></li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="并发编程概览">并发编程概览</h2><p><img src="/img/2015/并发编程.png" alt="并发编程"></p>
<h3 id="1,容器">1,容器</h3><p>略</p>
<h3 id="2，协作">2，协作</h3><p>略</p>
]]>
    </summary>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[java内存模型]]></title>
    <link href="http://sqtds.github.io/2015/05/10/2015/jmm/"/>
    <id>http://sqtds.github.io/2015/05/10/2015/jmm/</id>
    <published>2015-05-10T00:10:03.000Z</published>
    <updated>2015-05-10T13:41:48.000Z</updated>
    <content type="html"><![CDATA[<h2 id="JMM内存概览">JMM内存概览</h2><p><img src="/img/2015/JMM.png" alt=""></p>
<h3 id="参考资料">参考资料</h3><ul>
<li><a href="http://www.importnew.com/1993.html" target="_blank" rel="external">成为JavaGC专家Part I — 深入浅出Java垃圾回收机制</a></li>
<li><a href="&quot;http://www.importnew.com/2057.html&quot;">成为JavaGC专家Part II — 如何监控Java垃圾回收机制 </a></li>
<li><a href="http://www.importnew.com/3146.html" target="_blank" rel="external">成为Java GC专家系列Part III–如何优化Java垃圾回收机制 </a></li>
<li><a href="http://www.importnew.com/3151.html" target="_blank" rel="external">成为Java GC专家（4）—Apache的MaxClients参数详解及其在Tomcat执行FullGC时的影响</a></li>
<li><a href="http://www.importnew.com/13954.html" target="_blank" rel="external">成为Java GC专家（5）—Java性能调优原则</a></li>
<li><a href="http://ju.outofmemory.cn/entry/65368" target="_blank" rel="external">G1垃圾收集器</a></li>
<li><a href="http://ifeve.com/jvm-permgen-where-art-thou/" target="_blank" rel="external">JVM的持久代——何去何从？</a></li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="JMM内存概览">JMM内存概览</h2><p><img src="/img/2015/JMM.png" alt=""></p>
<h3 id="参考资料">参考资料</h3><ul>
<li><a href="http://www.importnew.com/1]]>
    </summary>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="memory" scheme="http://sqtds.github.io/tags/memory/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title></title>
    <link href="http://sqtds.github.io/2015/04/29/2014/linux-command/"/>
    <id>http://sqtds.github.io/2015/04/29/2014/linux-command/</id>
    <published>2015-04-29T08:35:07.000Z</published>
    <updated>2015-04-29T08:36:40.000Z</updated>
    <content type="html"><![CDATA[<h3 id="TOP命令">TOP命令</h3><p>对于多核CPU，显示的是多个CPU的所占的百分比的总和，如需要查看每个核的情况，进入top视图后按1，就会按核来显示消耗情况。<br>默认情况下，TOP视图中显示的是进程的CPU消耗情况，在TOP视图中，按shift+h后，可按线程查看CPU的消耗情况。</p>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="TOP命令">TOP命令</h3><p>对于多核CPU，显示的是多个CPU的所占的百分比的总和，如需要查看每个核的情况，进入top视图后按1，就会按核来显示消耗情况。<br>默认情况下，TOP视图中显示的是进程的CPU消耗情况，在TOP视图中，按shift+h后，]]>
    </summary>
    
  </entry>
  
  <entry>
    <title><![CDATA[性能调优总结]]></title>
    <link href="http://sqtds.github.io/2015/04/27/2014/%E6%80%A7%E8%83%BD%E8%B0%83%E4%BC%98%E6%80%BB%E7%BB%93/"/>
    <id>http://sqtds.github.io/2015/04/27/2014/性能调优总结/</id>
    <published>2015-04-27T03:38:06.000Z</published>
    <updated>2015-05-09T04:12:09.000Z</updated>
    <content type="html"><![CDATA[<p>性能调优主要是对CPU，磁盘IO，网络IO，内存等方面进行调优。<br><img src="/img/2014/性能调优.png" alt="性能调优"></p>
<h2 id="一，CPU">一，CPU</h2><h3 id="1,上下文切换">1,上下文切换</h3><p>上下文切换（有时也称做进程切换或任务切换）是指 CPU 从一个进程或线程切换到另一个进程或线程。</p>
<p>上下文是指某一时间点 CPU 寄存器和程序计数器的内容。寄存器是 CPU 内部的数量较少但是速度很快的内存（与之对应的是 CPU 外部相对较慢的 RAM 主内存）。寄存器通过对常用值（通常是运算的中间值）的快速访问来提高计算机程序运行的速度。程序计数器是一个专用的寄存器，用于表明指令序列中 CPU 正在执行的位置，存的值为正在执行的指令的位置或者下一个将要被执行的指令的位置，具体依赖于特定的系统。</p>
<p>稍微详细描述一下，上下文切换可以认为是内核（操作系统的核心）在 CPU 上对于进程（包括线程）进行以下的活动： </p>
<ol>
<li>挂起一个进程，将这个进程在 CPU 中的状态（上下文）存储于内存中的某处，</li>
<li>在内存中检索下一个进程的上下文并 将其在 CPU 的寄存器中恢复，</li>
<li>跳转到程序计数器所指向的位置（即跳转到进程被中断时的代码行），以恢复该进程。</li>
</ol>
<p>上下文切换的消耗</p>
<p>上下文切换通常是计算密集型的。也就是说，它需要相当可观的处理器时间，在每秒几十上百次的切换中，每次切换都需要纳秒量级的时间。所以，上下文切换对系统来说意味着消耗大量的 CPU 时间，事实上，可能是操作系统中时间消耗最大的操作。<br>Linux 号称的相比于其他的操作系统（包括其他类 Unix 系统）的很多优点中，有一项就是，其上下文切换和模式切换的时间消耗非常少。</p>
<h3 id="2，利用率">2，利用率</h3><p>利用率为CPU在用户进程，内核，中断处理，IO等待以及空闲5个部分使用百分比。Linux And System Network Performance Monitoring建议用户进程和内核进程的CPU消耗分别是65%-70%，30%-35%。<br>在liunx中可以使用top或者pidstat查看CPU的消耗情况。<br>在java应用中，CPU消耗主要在us,sy上。</p>
<h4 id="US">US</h4><p><strong>US:用户进程处理所占的百分比</strong><br>当us过高时，表明应用消耗了大量的CPU。在这种情况下，需要找到具体消耗CPU的线程所执行的代码。</p>
<h4 id="SY">SY</h4><p><strong>SY：内核线程所占的百分比</strong><br>当sy过高时，表示linux花费了过多的时间在进程切换上面。java应用造成这种现象的主要原因是启动的线程比较多，且这些线程多数处于不断阻塞的状态和执行状态的变化过程中，产生了大量的上下文切换。<br>通过jstack -l pid 查看线程争用。可以看到TIMED_WAITING(on object monitor)状态和Runnable转换中。</p>
<h2 id="二，磁盘IO">二，磁盘IO</h2><p>在linux中，要查看线程的磁盘IO消耗，主要方法是通过pidstat。<br><img src="/img/2014/QQ图片20150429165052.png" alt=""><br>使用pidstat找到文件IO操作多的线程，之后结合jstack找到对应的java代码。</p>
<h2 id="三，网络IO">三，网络IO</h2><p>在linux中，通过sar来分析网络IO的状况。如果需要跟踪tcp的通信信息，需要使用tcpdump来进行。</p>
<h2 id="四，内存消耗">四，内存消耗</h2><p>通过jmap，jstat，mat，visualVM来分析</p>
<p>PS:本文总结自《分布式JAVA应用 基础与实践》—-林昊</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>性能调优主要是对CPU，磁盘IO，网络IO，内存等方面进行调优。<br><img src="/img/2014/性能调优.png" alt="性能调优"></p>
<h2 id="一，CPU">一，CPU</h2><h3 id="1,上下文切换">1,上下文切换</h3><]]>
    </summary>
    
      <category term="性能" scheme="http://sqtds.github.io/tags/%E6%80%A7%E8%83%BD/"/>
    
      <category term="性能" scheme="http://sqtds.github.io/categories/%E6%80%A7%E8%83%BD/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[String字符串小结]]></title>
    <link href="http://sqtds.github.io/2015/04/26/2014/java-string/"/>
    <id>http://sqtds.github.io/2015/04/26/2014/java-string/</id>
    <published>2015-04-26T04:02:33.000Z</published>
    <updated>2015-04-26T10:13:41.000Z</updated>
    <content type="html"><![CDATA[<p>String可以说是java编程中最常用的对象。用了这么久，大家对它的印象有多深呢？下面我就谈谈我自己对它的一些认知吧。</p>
<h2 id="不可变对象">不可变对象</h2><p>我们可以看到，String被声明成了final,这样我们可以得到String是不可继承的对象，也是线程安全的对象。</p>
<pre><code><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">String</span>
<span class="keyword">implements</span> <span class="title">java</span>.<span class="title">io</span>.<span class="title">Serializable</span>, <span class="title">Comparable</span>&lt;<span class="title">String</span>&gt;, <span class="title">CharSequence</span> </span>{
    <span class="comment">/** The value is used for character storage. */</span>
    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">char</span> value[];

    <span class="comment">/** Cache the hash code for the string */</span>
    <span class="keyword">private</span> <span class="keyword">int</span> hash; <span class="comment">// Default to 0</span>
}
</code></pre><h3 id="concat">concat</h3><p>接下来我们看String的concat方法。由于String是不可变的，所以当我们连接一个字符串时，它会合并2哥字符串的value数组，然后用这个数组创建一个新的字符串。</p>
<pre><code><span class="keyword">public</span> <span class="keyword">String</span> <span class="built_in">concat</span>(<span class="keyword">String</span> <span class="built_in">str</span>) {
    <span class="built_in">int</span> otherLen = <span class="built_in">str</span>.length();
    <span class="keyword">if</span> (otherLen == <span class="number">0</span>) {
        <span class="keyword">return</span> <span class="keyword">this</span>;
    }
    <span class="built_in">int</span> len = value.length;
    <span class="built_in">char</span> buf[] = Arrays.copyOf(value, len + otherLen);
    <span class="built_in">str</span>.getChars(buf, len);
    <span class="keyword">return</span> <span class="keyword">new</span> <span class="keyword">String</span>(buf, <span class="keyword">true</span>);
  }
</code></pre><h3 id="“+”连接符的处理">“+”连接符的处理</h3><p>Java 语言提供对字符串串联符号（”+”）以及将其他对象转换为字符串的特殊支持。字符串串联是通过 StringBuilder（或 StringBuffer）类及其 append 方法实现的。</p>
<pre><code><span class="keyword">String</span> str1 = <span class="string">"aabc0"</span>;
<span class="keyword">String</span> str2 = <span class="string">"aa"</span> + <span class="string">"bc"</span>+<span class="string">"0"</span>;
System.out.<span class="built_in">println</span>(str1+<span class="string">"  "</span>+str2);
System.out.<span class="built_in">println</span>(str1==str2);
<span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; <span class="number">1</span>; i++) {
    <span class="keyword">String</span> str3 = <span class="string">"aa"</span> + <span class="string">"bc"</span>+i;
    System.out.<span class="built_in">println</span>(str1+<span class="string">"  "</span>+str3);
    System.out.<span class="built_in">println</span>(str1==str3);
}
</code></pre><p>打印结果：</p>
<pre><code><span class="title">aabc0</span>  aabc0
<span class="built_in">true</span>
aabc0  aabc0
<span class="built_in">false</span>
</code></pre><p>如果在编译期间就能确定字符串，编译器就会优化String，合并到一起。如上例所示str1==str2为true，表明为同一字符串。</p>
<h2 id="intern浅析">intern浅析</h2><p>对于字符串常量池，看了网上的很多文章，大部分人貌似都不对。现在在此在总结一遍。</p>
<pre><code><span class="keyword">String</span> str1 = <span class="keyword">new</span> StringBuilder(<span class="string">"chaofan"</span>).<span class="built_in">append</span>(<span class="string">"wei"</span>).toString();
System.out.<span class="built_in">println</span>(str1.intern() == str1);

<span class="keyword">String</span> str2 = <span class="keyword">new</span> StringBuilder(<span class="string">"ja"</span>).<span class="built_in">append</span>(<span class="string">"va"</span>).toString();
System.out.<span class="built_in">println</span>(str2.intern() == str2);
</code></pre><p>打印结果：</p>
<pre><code><span class="title">jdk6</span> 下<span class="built_in">false</span> <span class="built_in">false</span>
jdk7 下<span class="built_in">true</span> <span class="built_in">false</span>
</code></pre><p>产生的差异在于在jdk1.6中 intern 方法会把首次遇到的字符串实例复制到永久待（常量池）中，并返回此引用；但在jdk1.7中，只是会把首次遇到的字符串实例的引用添加到常量池中（没有复制），并返回此引用。<br>所以在jdk1.7中执行上面代码，str1返回true是引用他们指向的都是str1对象（堆中）（池中不存在，返回原引用），而str2返回false是因为池中已经存在”java”了（关键词），所以返回的池的对象，因此不相等。</p>
<p>相信很多 JAVA 程序员都做做类似 String s = new String(“abc”)这个语句创建了几个对象的题目。 这种题目主要就是为了考察程序员对字符串对象的常量池掌握与否。上述的语句中是创建了2个对象，第一个对象是”abc”字符串存储在常量池中，第二个对象在JAVA Heap中的 String 对象。接下来我们分析另一端代码。</p>
<pre><code><span class="keyword">String</span> s = <span class="keyword">new</span> <span class="keyword">String</span>(<span class="string">"1"</span>);
s.intern();
<span class="keyword">String</span> s2 = <span class="string">"1"</span>;
System.out.<span class="built_in">println</span>(s == s2);

<span class="keyword">String</span> s3 = <span class="keyword">new</span> <span class="keyword">String</span>(<span class="string">"1"</span>) + <span class="keyword">new</span> <span class="keyword">String</span>(<span class="string">"1"</span>);
s3.intern();
<span class="keyword">String</span> s4 = <span class="string">"11"</span>;
System.out.<span class="built_in">println</span>(s3 == s4);
</code></pre><p>打印结果是</p>
<ul>
<li>jdk6 下<code>false false</code></li>
<li>jdk7 下<code>false true</code></li>
</ul>
<p>为什么会这样呢？美团网给出了很好的解释？我觉得上面的说法比美团网的解释更好，大家可以参考<a href="http://tech.meituan.com/in_depth_understanding_string_intern.html。" target="_blank" rel="external">http://tech.meituan.com/in_depth_understanding_string_intern.html。</a><br>最根本的原因应该是:<br>jdk7 版本对 intern 操作和常量池都做了一定的修改。主要包括2点：</p>
<ul>
<li>将String常量池 从 Perm 区移动到了 Java Heap区</li>
<li>String#intern 方法时，如果存在堆中的对象，会直接保存对象的引用，而不会重新创建对象。</li>
</ul>
<h3 id="intern需要注意的地方">intern需要注意的地方</h3><p>String的String Pool是一个固定大小的Hashtable，<strong>默认值大小长度是1009</strong>，如果放进String Pool的String非常多，就会造成Hash冲突严重，从而导致链表会很长，而链表长了后直接会造成的影响就是当调用String.intern时性能会大幅下降（因为要一个一个找）。</p>
<p>在 jdk6中StringTable是固定的，就是1009的长度，所以<strong>如果常量池中的字符串过多就会导致效率下降很快</strong>。在jdk7中，StringTable的长度可以通过一个参数指定：</p>
<ul>
<li>-XX:StringTableSize=99991</li>
</ul>
<p>例如：在fastjson 中对所有的 json 的 key 使用了 intern 方法，缓存到了字符串常量池中，这样每次读取的时候就会非常快，大大减少时间和空间。而且 json 的 key 通常都是不变的。这个地方没有考虑到大量的 json key 如果是变化的，那就会给字符串常量池带来很大的负担。</p>
<p>这个问题 fastjson 在1.1.24版本中已经将这个漏洞修复了。程序加入了一个最大的缓存大小，超过这个大小后就不会再往字符串常量池中放了。<br>更多详情请参考<a href="http://tech.meituan.com/in_depth_understanding_string_intern.html" target="_blank" rel="external">http://tech.meituan.com/in_depth_understanding_string_intern.html</a></p>
<h2 id="subString内存泄露">subString内存泄露</h2><p>首先我们看subString方法。</p>
<pre><code><span class="function"><span class="keyword">public</span> String <span class="title">substring</span><span class="params">(<span class="keyword">int</span> beginIndex)</span> </span>{
    <span class="keyword">if</span> (beginIndex &lt; <span class="number">0</span>) {
        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(beginIndex);
    }
    <span class="keyword">int</span> subLen = <span class="keyword">value</span>.length - beginIndex;
    <span class="keyword">if</span> (subLen &lt; <span class="number">0</span>) {
        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(subLen);
    }
    <span class="keyword">return</span> (beginIndex == <span class="number">0</span>) ? <span class="keyword">this</span> : <span class="keyword">new</span> String(<span class="keyword">value</span>, beginIndex, subLen);
}
</code></pre><p>在这个方法中，新建了一个String对象。</p>
<p>在jdk1.7,1.8中,如下：</p>
<pre><code><span class="function"><span class="keyword">public</span> <span class="title">String</span><span class="params">(<span class="keyword">char</span> <span class="keyword">value</span>[], <span class="keyword">int</span> offset, <span class="keyword">int</span> count)</span> </span>{
     ....
    <span class="keyword">this</span>.<span class="keyword">value</span> = Arrays.copyOfRange(<span class="keyword">value</span>, offset, offset+count);
}
</code></pre><p>在jdk1.6中</p>
<pre><code>String(<span class="keyword">int</span> offset, <span class="keyword">int</span> <span class="keyword">count</span>, <span class="keyword">char</span> value[]) {
    <span class="keyword">this</span>.value = value;
    <span class="keyword">this</span>.offset = offset;
    <span class="keyword">this</span>.<span class="keyword">count</span> = <span class="keyword">count</span>;
}
</code></pre><p>我们可以看到，1.6中还是引用了原来的字符串，存在内存泄露问题。1.7及以上使用了数组复制的方法创建了一个新的数组，不存在内存泄露。</p>
<h2 id="其他">其他</h2><h3 id="字符串分割">字符串分割</h3><p>在jdk1.6中：优先考虑使用indexOf方法，最后才使用spilt方法。<br>在jdk1.8中：优先考虑spilt方法，最后是indexOf方法。<br>为什么？我们对下面代码进行测试：</p>
<pre><code>StringBuffer sb = new StringBuffer<span class="list">()</span><span class="comment">;</span>
int i = <span class="number">1000</span><span class="comment">;</span>
for <span class="list">(<span class="keyword">int</span> i1 = i<span class="comment">; i1 &gt; 0; i1--) {</span>
    sb.append<span class="list">(<span class="keyword">i1</span>)</span>.append<span class="list">(<span class="string">";"</span>)</span><span class="comment">;</span>
}
String old = sb.toString<span class="list">()</span><span class="comment">;</span>
int k = <span class="number">10000</span><span class="comment">;</span>
long l = System.currentTimeMillis<span class="list">()</span><span class="comment">;</span>
for <span class="list">(<span class="keyword">int</span> k1 = k<span class="comment">; k1 &gt; 0; k1--) {</span>
    old.split<span class="list">(<span class="string">";"</span>)</span><span class="comment">;</span>
}
System.out.println<span class="list">(<span class="keyword">System</span>.currentTimeMillis<span class="list">()</span> - l)</span><span class="comment">;</span>
long l1 = System.currentTimeMillis<span class="list">()</span><span class="comment">;</span>
String tmp = old<span class="comment">;</span>
for <span class="list">(<span class="keyword">int</span> k1 = k<span class="comment">; k1 &gt; 0; k1--) {</span>
    while <span class="list">(<span class="keyword">true</span>)</span> {
        int j = tmp.indexOf<span class="list">(<span class="string">";"</span>)</span><span class="comment">;</span>
        if <span class="list">(<span class="keyword">j</span> &lt; <span class="number">0</span>)</span> {
            break<span class="comment">;</span>
        }
        tmp.substring<span class="list">(<span class="number">0</span>, j)</span><span class="comment">;</span>
        tmp = tmp.substring<span class="list">(<span class="keyword">j</span> + <span class="number">1</span>)</span><span class="comment">;</span>
    }
    tmp = old<span class="comment">;</span>
}
System.out.println<span class="list">(<span class="keyword">System</span>.currentTimeMillis<span class="list">()</span> - l1)</span><span class="comment">;</span></span></span></span>
</code></pre><p>结果如下：</p>
<ul>
<li>jdk1.6中 <code>1073 295</code>    </li>
<li>jdk1.8中 <code>606 7177</code></li>
</ul>
<p>对比1073与606，在1.8中由于取消了offset和count2个字段，所以spilt的性能有了较大的提升。<br>对比295与7177，在1.6中，由于substring方法不会产生新字符串，所以速度更快。在1.8中每次spilt都会产生新的字符串，造成了性能瓶颈。</p>
<h3 id="startWith替代">startWith替代</h3><p>我们看看下面一段代码：</p>
<pre><code><span class="keyword">String</span> ss = <span class="string">"abcdefgg"</span>;
<span class="built_in">int</span> i = <span class="number">10000000</span>;
<span class="keyword">long</span> l = System.currentTimeMillis();
<span class="keyword">for</span> (<span class="built_in">int</span> i1 = i; i1 &gt; <span class="number">0</span>; i1--) {
    <span class="built_in">boolean</span> a = ss.startsWith(<span class="string">"abc"</span>);
}
System.out.<span class="built_in">println</span>(System.currentTimeMillis() - l);
<span class="keyword">long</span> l1 = System.currentTimeMillis();
<span class="keyword">for</span> (<span class="built_in">int</span> i1 = i; i1 &gt; <span class="number">0</span>; i1--) {
    <span class="built_in">boolean</span> c = ss.charAt(<span class="number">0</span>)==<span class="string">'a'</span>&amp;&amp;ss.charAt(<span class="number">1</span>)==<span class="string">'b'</span>&amp;&amp;ss.charAt(<span class="number">2</span>)==<span class="string">'c'</span>;
}
System.out.<span class="built_in">println</span>(System.currentTimeMillis() - l1);
</code></pre><p>打印结果如下：</p>
<ul>
<li>在jdk1.6中 <code>109 26</code></li>
<li>在jdk1.8中 <code>22 21</code></li>
</ul>
<p>为什么会出现这样的结果呢，我们分析代码，发现</p>
<pre><code><span class="comment">//1.6</span>
<span class="function"><span class="keyword">public</span> boolean <span class="title">startsWith</span><span class="params">(String prefix, <span class="keyword">int</span> toffset)</span> </span>{
    <span class="keyword">char</span> ta[] = <span class="keyword">value</span>;
    <span class="keyword">int</span> to = offset + toffset;
    <span class="keyword">char</span> pa[] = prefix.<span class="keyword">value</span>;
    <span class="keyword">int</span> po = prefix.offset;
    <span class="keyword">int</span> pc = prefix.count;
    <span class="comment">// Note: toffset might be near -1&gt;&gt;&gt;1.</span>
    <span class="keyword">if</span> ((toffset &lt; <span class="number">0</span>) || (toffset &gt; count - pc)) {
        <span class="keyword">return</span> <span class="keyword">false</span>;
    }
    <span class="keyword">while</span> (--pc &gt;= <span class="number">0</span>) {
        <span class="keyword">if</span> (ta[to++] != pa[po++]) {
            <span class="keyword">return</span> <span class="keyword">false</span>;
        }
    }
    <span class="keyword">return</span> <span class="keyword">true</span>;
}

<span class="comment">//1.8</span>
<span class="function"><span class="keyword">public</span> boolean <span class="title">startsWith</span><span class="params">(String prefix, <span class="keyword">int</span> toffset)</span> </span>{
    <span class="keyword">char</span> ta[] = <span class="keyword">value</span>;
    <span class="keyword">int</span> to = toffset;
    <span class="keyword">char</span> pa[] = prefix.<span class="keyword">value</span>;
    <span class="keyword">int</span> po = <span class="number">0</span>;
    <span class="keyword">int</span> pc = prefix.<span class="keyword">value</span>.length;
    <span class="comment">// Note: toffset might be near -1&gt;&gt;&gt;1.</span>
    <span class="keyword">if</span> ((toffset &lt; <span class="number">0</span>) || (toffset &gt; <span class="keyword">value</span>.length - pc)) {
        <span class="keyword">return</span> <span class="keyword">false</span>;
    }
    <span class="keyword">while</span> (--pc &gt;= <span class="number">0</span>) {
        <span class="keyword">if</span> (ta[to++] != pa[po++]) {
            <span class="keyword">return</span> <span class="keyword">false</span>;
        }
    }
    <span class="keyword">return</span> <span class="keyword">true</span>;
}
</code></pre><p>在1.6中第四行，<code>int to = offset + toffset;</code>进行了整形相加操作，这里影响了方法的执行时间。1.7及以后去掉了offset和count，性能有了极大的提升。</p>
<h2 id="参考资料">参考资料</h2><ul>
<li><a href="http://blog.csdn.net/chaofanwei/article/details/19486919" target="_blank" rel="external">深入理解java虚拟机（三）：String.intern()-字符串常量池</a></li>
<li><a href="http://tech.meituan.com/in_depth_understanding_string_intern.html" title="深入解析String#intern" target="_blank" rel="external">http://tech.meituan.com/in_depth_understanding_string_intern.html</a></li>
<li>《java程序性能优化-让你的程序更快更稳定》</li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<p>String可以说是java编程中最常用的对象。用了这么久，大家对它的印象有多深呢？下面我就谈谈我自己对它的一些认知吧。</p>
<h2 id="不可变对象">不可变对象</h2><p>我们可以看到，String被声明成了final,这样我们可以得到String是不可继承的]]>
    </summary>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="string" scheme="http://sqtds.github.io/tags/string/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[java集合小结]]></title>
    <link href="http://sqtds.github.io/2015/04/25/2014/collection-total/"/>
    <id>http://sqtds.github.io/2015/04/25/2014/collection-total/</id>
    <published>2015-04-25T08:37:30.000Z</published>
    <updated>2015-05-09T04:12:09.000Z</updated>
    <content type="html"><![CDATA[<p>本文主要从整体架构，功能特性，和其他方面介绍集合。</p>
<h2 id="一-整体架构">一.整体架构</h2><p>上图：<br><img src="/img/2014/collections.png" alt="集合架构"><br>我们可以看到集合主要分为set，list和queue这三类，似乎还有一个Stack。它在哪里呢？<br>我们看JDK代码：<code>public class Stack&lt;E&gt; extends Vector&lt;E&gt;</code>，原来它继承了Vector。</p>
<p>看上面这个图，我们是不是有点疑问，为什么Linkedlist实现了List又实现了Queue。这是因为List可以模拟Queue的行为，所以也可以将他看成是Queue对象，只需要去掉List的随机访问需求即可，从而实现更高效的并发。</p>
<p>了解了Collection后，我们发现JDK中还有个Collections，它是什么鬼东西？查看源码，我们发现它是包含了对Collection的基本静态方法，例如排序，创建，复制等。同理，Arrays是对数组的操作，Executors是对Executor的操作。有没有一种感觉写jdk的人喜欢用名称加s来代表一个utils。</p>
<h3 id="SET">SET</h3><p>一个不包括重复元素（包括可变对象）的Collection，是一种无序的集合。Set不包含满 a.equals(b) 的元素对a和b，并且最多有一个null。实现Set的接口有：EnumSet、HashSet、TreeSet等。下图是Set的JDK源码UML图。<br><img src="/img/2014/collection-set.png" alt="set"></p>
<h3 id="List">List</h3><p>一个有序的Collection（也称序列），元素可以重复。确切的讲，列表通常允许满足 e1.equals(e2) 的元素对 e1 和 e2，并且如果列表本身允许 null 元素的话，通常它们允许多个 null 元素。实现List的有：ArrayList、LinkedList、Vector、Stack等。下图是List的JDK源码UML图。<br><img src="/img/2014/collection-list.png" alt="list"></p>
<h3 id="Queue">Queue</h3><p>一种队列则是双端队列，支持在头、尾两端插入和移除元素，主要包括：ArrayDeque、LinkedBlockingDeque、LinkedList。另一种是阻塞式队列，队列满了以后再插入元素则会抛出异常，主要包括ArrayBlockQueue、PriorityBlockingQueue、LinkedBlockingQueue。虽然接口并未定义阻塞方法，但是实现类扩展了此接口。下图是Queue的JDK源码UML图。<br><img src="/img/2014/collection-queue.png" alt="queue"></p>
<h2 id="二，功能特性">二，功能特性</h2><p>以下比较基于ArrayList，linkedList，Vector，Stack,HashSet,TreeSet,HashMap,TreeMap</p>
<h3 id="1，添加元素">1，添加元素</h3><p>在添加元素上，ArrayList，Vector的性能相对较差，原因是ArrayList在容量不够时需要扩充。</p>
<h3 id="2，查找元素">2，查找元素</h3><p>在查找元素上，ArrayList，linkedList，Vector，Stack的性能略差一点点，这是由于他们在查找时需要遍历整个集合。而Set，Map类型的都是通过hash后再在链表上查找，因此速度比较快。</p>
<h3 id="3，删除元素">3，删除元素</h3><p>在删除元素上，除了TreeMap和TreeSet外，其他集合类型的性能基本无差异，TreeSet基于TreeMap实现，TreeMap之所以性能相对较差的原因是它在删除时需要排序。<br>List适用于允许重复元素的单个对象集合场景，Set适用于不允许重复元素的单个对象集合场景，Map适合key-value结构的集合场景。</p>
<h2 id="三，并发结构">三，并发结构</h2><h3 id="1，ConcurrentHashMap">1，ConcurrentHashMap</h3><p>线程安全的haspmap，默认采用16个Segment存储对象。每个Segment一把锁，可允许16个线程同时并发的操作集合对象。</p>
<h3 id="2，CopyOnWriteArrayList">2，CopyOnWriteArrayList</h3><p>CopyOnWriteArrayList是一个线程安全的，并且在读操作时无需加锁的ArrayList。</p>
<h3 id="3，ArrayBlockingQueue">3，ArrayBlockingQueue</h3><p>ArrayBlockingQueue是一个基于数组，先进先出的，线程安全的集合类，其特色为指定时间的阻塞读写，并且容量是可限制的。</p>
<blockquote>
<p>offer(E,long,TimeUnit) 插入元素到数组的尾部，如果数组已满，则等待。poll(E,long,TimeUnit)获取数组中的第一个元素，如果数组中没有元素，则等待。</p>
</blockquote>
<h3 id="4,LinkedBlockingQueue">4,LinkedBlockingQueue</h3><p>LinkedBlockingQueue采用链表的方式存储对象。</p>
<blockquote>
<p>对于读操作take和poll，采用一把锁，对于写操作put和poll，采用另一把锁。因此在高并发读写操作多的情况下，性能好于ArrayBlockingQueue。但在遍历和删除元素时，需要2把锁同时锁住。</p>
</blockquote>
<h3 id="5,IndentityHashMap">5,IndentityHashMap</h3><p>简单说IdentityHashMap与常用的HashMap的区别是：前者比较key时是“引用相等”而后者是“对象相等”，即对于k1和k2，当k1==k2时，IdentityHashMap认为两个key相等，而HashMap只有在k1.equals(k2) == true 时才会认为两个key相等。</p>
<h3 id="6,ConcurrentSkipListMap">6,ConcurrentSkipListMap</h3><p>SkipList是红黑树的一种简化替代方案，是个流行的有序集合算法。在4线程1.6万数据的条件下，ConcurrentHashMap 存取速度是ConcurrentSkipListMap 的4倍左右。<br>但ConcurrentSkipListMap有几个ConcurrentHashMap 不能比拟的优点：</p>
<ol>
<li>ConcurrentSkipListMap 的key是有序的。</li>
<li>ConcurrentSkipListMap 支持更高的并发。ConcurrentSkipListMap 的存取时间是log（N），和线程数几乎无关。也就是说在数据量一定的情况下，并发的线程越多，ConcurrentSkipListMap越能体现出他的优势。 </li>
</ol>
<h2 id="四，其他特性">四，其他特性</h2><h3 id="算法">算法</h3><ol>
<li>Colletions.sort(list) 与 Arrays.sort(T[]) </li>
</ol>
<p>Colletions.sort()实际会将list转为数组，然后调用Arrays.sort()，排完了再转回List。<br>PS. JDK8里，List有自己的sort()方法了，像ArrayList就直接用自己内部的数组来排，而LinkedList, CopyOnWriteArrayList还是要复制出一份数组。<br>而Arrays.sort()，对原始类型(int[],double[],char[],byte[])，JDK6里用的是快速排序，对于对象类型(Object[])，JDK6则使用归并排序。为什么要用不同的算法呢？</p>
<ol>
<li>JDK7的进步</li>
</ol>
<p>到了JDK7，快速排序升级为双基准快排(双基准快排 vs 三路快排)；归并排序升级为归并排序的改进版TimSort，一个JDK的自我进化。</p>
<ol>
<li>JDK8的进步</li>
</ol>
<p>再到了JDK8， 对大集合增加了Arrays.parallelSort()函数，使用fork-Join框架，充分利用多核，对大的集合进行切分然后再归并排序，而在小的连续片段里，依然使用TimSort与DualPivotQuickSort。</p>
<h3 id="五，参考资料">五，参考资料</h3><ol>
<li>《分布式JAVA应用 基础与实践》—-林昊</li>
<li><a href="http://calvin1978.blogcn.com/articles/jdk.html" target="_blank" rel="external">JDK78数则</a></li>
<li><a href="http://www.bysocket.com/?p=162" target="_blank" rel="external">Java 容器 &amp; 泛型（1）：认识容器</a></li>
</ol>
]]></content>
    <summary type="html">
    <![CDATA[<p>本文主要从整体架构，功能特性，和其他方面介绍集合。</p>
<h2 id="一-整体架构">一.整体架构</h2><p>上图：<br><img src="/img/2014/collections.png" alt="集合架构"><br>我们可以看到集合主要分为set，li]]>
    </summary>
    
      <category term="collection" scheme="http://sqtds.github.io/tags/collection/"/>
    
      <category term="j2se" scheme="http://sqtds.github.io/tags/j2se/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[mysql索引总结]]></title>
    <link href="http://sqtds.github.io/2015/04/25/2014/mysql-index/"/>
    <id>http://sqtds.github.io/2015/04/25/2014/mysql-index/</id>
    <published>2015-04-25T08:26:28.000Z</published>
    <updated>2015-04-26T03:55:48.000Z</updated>
    <content type="html"><![CDATA[<p>索引是什么？索引是存储引擎用于快速找到记录的一种数据结构。</p>
<h2 id="索引分类">索引分类</h2><h3 id="B-Tree索引">B-Tree索引</h3><p>MyISAM使用前缀压缩技术使得索引更小。InnoDB按照原数据存储索引。<br>MyISAM索引通过数据的物理位置引用被索引的行。InnoDB根据主键引用被索引的行。<br>可以使用B-Tree索引的查询类型：</p>
<ul>
<li>全值匹配：和索引中的所有列进行匹配</li>
<li>匹配最左前缀：匹配第一个索引</li>
<li>匹配列前缀：匹配某一列的值的开头部分，只使用第一个索引</li>
<li>匹配范围值：匹配某个范围，只使用第一个索引</li>
<li>精确匹配某一列并范围匹配另外一列</li>
<li>只访问索引的查询</li>
<li>排序操作：索引的有序性</li>
</ul>
<h3 id="哈希索引">哈希索引</h3><p>哈希索引基于哈希表实现，只有精确匹配所有列的查询才有效。对于每一行数据，存储引擎会对所有的索引列计算一个hash值。<br>哈希索引的限制：</p>
<ul>
<li>哈希索引只能包含哈希值和行指针，不能使用索引的值避免行读取。</li>
<li>哈希索引数据并不是按照索引值顺序存储的，无法用于排序。</li>
<li>哈希索引也不支持部分索引列匹配查找，因为哈希索引始终是使用索引列的全部内容来计算哈希值的。</li>
<li>哈希索引只支持等值查找。</li>
<li>访问哈希索引的数据非常快，除非有很多哈希冲突。</li>
</ul>
<h3 id="空间数据索引">空间数据索引</h3><p>地理数据索引。（略）</p>
<h3 id="全文索引">全文索引</h3><p>全文索引查找文本中的关键词，一般采用倒排索引。</p>
<h3 id="其他">其他</h3><h2 id="索引的优点">索引的优点</h2><p>索引可以让服务器快速的定位到指定的位置。</p>
<ul>
<li>索引大大减少了服务器需要扫描的数量。</li>
<li>索引可以帮助服务器避免排序和临时表。</li>
<li>索引可以将随机IO变为顺序IO。</li>
</ul>
<p>参考:Relational Database Index Design and the Optimizers.</p>
<p>索引是最好的解决方案吗？<br>对于非常小的表，大部分情况下简单的全表扫描更高效。<br>对于中大型的表，索引就非常有效。<br>对于特大型的表，建立和使用索引的代价随之增长。可以采用分区技术等。</p>
<h2 id="索引策略">索引策略</h2><h3 id="独立的列">独立的列</h3><p>索引列不能是表达式的一部分，也不能使函数的参数。</p>
<h3 id="前缀索引和索引选择性">前缀索引和索引选择性</h3><p>有时候需要索引很长的字符列，会让索引变得很大，这是我们可以使用哈希索引。我们是否还有更好的方法？</p>
<p>通常可以索引开始的部分字符，这样可以节省索引空间，从而提高索引效率。但这样也会降低索引的选择性。</p>
<h3 id="多列索引">多列索引</h3><p>很多人对多列索引的理解都不够。一个常见的错误就是，为每个列创建独立的索引，或者按照错误的顺序创建多列索引。</p>
<p>在多个列上建立独立的单列索引大部分情况下并不能提高MySql的查询性能。MySql5.0以上引入了一种“索引合并”的策略，一定程度上可以使用表上的多个单列索引来定位指定的行。</p>
<h3 id="选择合适的索引列顺序">选择合适的索引列顺序</h3><p>经验法则：将选择性最高的列放在索引最前列。</p>
<h3 id="聚簇索引">聚簇索引</h3><p>聚簇索引并不是一种单独的索引类型，而是一种数据存储方式。InnoDB的聚簇索引索引实际上在同一个结构中保存了B-Tree索引和物理行。<br>当表有聚簇索引时，实际上它的数据行保存在索引的叶子节点中。所以，一个表中只能有一个聚簇索引。</p>
<p>聚簇索引的优点：</p>
<ul>
<li>把相关数据保存在一起</li>
<li>数据访问更快，提高了IO的性能</li>
</ul>
<p>缺点：</p>
<ul>
<li>插入速度依赖于插入顺序。</li>
<li>更新索引的代价很高。</li>
<li>插入新行，更新主键或者移动行的时候，面临“页分裂”的问题。</li>
<li>可能导致全表扫描变慢，尤其是行稀疏，也分裂导致数据不连续</li>
<li>二级索引（非聚簇索引）可能比想象的大，因为包含了主键列。</li>
</ul>
<h3 id="覆盖索引">覆盖索引</h3><p>如果索引的叶子节点已经包含了需要查询的字段，那么还有什么必要再回表查询？如果一个索引包含所有需要查询的字段的值，我们就称为“覆盖索引”。</p>
<p>适应场景：</p>
<ul>
<li>索引条目通常远小于数据行的大小。</li>
<li>对于IO密集型的范围查询会比随机从磁盘读取每一行数据的IO要小的多。</li>
<li>可内存缓存索引，避免系统调用</li>
</ul>
<h3 id="使用索引进行排序">使用索引进行排序</h3><p>当索引的列序列和order by子句的顺序完全一致，并且所有列的排序方向（倒序或正序）都一样时，MySql才能够使用索引来对结果进行排序。如果查询关联多个表时，则只有当ORDER BY子句引用的字段全部为第一个表时，才能使用索引进行排序。ORDER BY 子句和查找型查询的限制是一样的：需要全部满足索引的最左前缀的要求；否则，MySql都需要执行排序操纵，而无法利用索引进行排序。</p>
<p>有一种情况下ORDER BY子句可以不满足索引的最左前缀要求，就是前导列为常量的时候。如果WHERE子句或者JOIN子句中对这些列指定了常量，就可以“弥补”索引的不足。</p>
<h3 id="索引和锁">索引和锁</h3><p>索引可以让查询锁定更少的行。如果你的查询不需要访问那些不需要的行，那么就会锁定更少的行。</p>
<h2 id="设计原则">设计原则</h2><h3 id="选择性特别低，并且经常用到的的作为索引前列">选择性特别低，并且经常用到的的作为索引前列</h3><p>例如性别只有2种选择，但是我们经常会用到。如果没有用到性别查询，我们也可以使用 sex in（‘m’，‘f’）来让mysql选择该索引。</p>
<p>有一点需要注意的是，使用in的方式覆盖不在where条件中查询的列，不能过度使用。因为每增加一个in条件，优化器都需要以指数的形式增加，a<em>b</em>c。</p>
<h3 id="对于经常性范围查询，应放到索引后列">对于经常性范围查询，应放到索引后列</h3><p>对于范围查询，MySql无法再使用范围列后面的其他索引了，但是对于多个“等值条件查询”则没有这个限制。<br>例如年龄查询，可能是查询某个范围的，我们就要放到后面。</p>
<h3 id="避免多个范围查询">避免多个范围查询</h3><p>使用in来代替多范围查询</p>
<h3 id="优化排序">优化排序</h3><p>对于选择性非常低的列，可以添加一些特殊的索引进行排序。<br>例如sex进行排序时，可以使用(sex,rating)索引进行排序。<br>select <cols> from table where sex=’M’ order by rating ;</cols></p>
<p>PS:本文总结来自《mysql高性能》一书</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>索引是什么？索引是存储引擎用于快速找到记录的一种数据结构。</p>
<h2 id="索引分类">索引分类</h2><h3 id="B-Tree索引">B-Tree索引</h3><p>MyISAM使用前缀压缩技术使得索引更小。InnoDB按照原数据存储索引。<br>MyISA]]>
    </summary>
    
      <category term="index" scheme="http://sqtds.github.io/tags/index/"/>
    
      <category term="mysql" scheme="http://sqtds.github.io/tags/mysql/"/>
    
      <category term="数据库" scheme="http://sqtds.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[tomcat架构]]></title>
    <link href="http://sqtds.github.io/2015/04/06/2014/tomcat-artic/"/>
    <id>http://sqtds.github.io/2015/04/06/2014/tomcat-artic/</id>
    <published>2015-04-06T04:35:21.000Z</published>
    <updated>2015-04-06T07:02:32.000Z</updated>
    <content type="html"><![CDATA[<p>tomcat虽然比较复杂，但是结构优良，下面让我们看看其良好的架构设计。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>tomcat虽然比较复杂，但是结构优良，下面让我们看看其良好的架构设计。</p>
]]>
    </summary>
    
      <category term="tomcat" scheme="http://sqtds.github.io/tags/tomcat/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Nosql精粹读书笔记（一）——聚合数据模型]]></title>
    <link href="http://sqtds.github.io/2015/04/05/2014/nosql-distilled/"/>
    <id>http://sqtds.github.io/2015/04/05/2014/nosql-distilled/</id>
    <published>2015-04-05T07:43:14.000Z</published>
    <updated>2015-05-09T04:12:09.000Z</updated>
    <content type="html"><![CDATA[<h2 id="聚合数据模型">聚合数据模型</h2><p><strong>数据模型是认知和操作数据时所用的模型。</strong>对于使用数据库的人来说， 数据模型描述了我们如何同数据库中的数据打交道。 它与存储模型不同， 后者描述了数据库内部存储及操作数据的机制。 </p>
<p>大家日常所说的“ 数据模型” 一词， 一般指应用程序的特定数据所具备的模型。 开发者可能会指着一张数据库的“ 实体 - 关系图 ”（ entity-relationship diagram）， 把这个包含客户、 订单、 产品等信息的东西叫做他们的数据模型。然而本书的“ 数据模型” 通常表示数据库组织数据的方式， 它的正式名称是“ 元模型”（ metamodel）。</p>
<p>NoSQL 技术与传统的关系型数据库相比， 一个最明显的转变就是抛弃了关系模型。 每种 NoSQL 解决方案的模型都不同， 本书把 NoSQL 生态系统中广泛使用的模型分为四类 ：“ 键值”、“ 文档”、“ 列族” 和“ 图”。 前三类数据模型有一个共同特征， 我们称其为“ 面向聚合”（ aggregate orientation）。</p>
<h3 id="聚合">聚合</h3><p><strong>关系模型把待存储的信息分隔成元组（ 行）。</strong> 元组是种受限的数据结构 ： 它只能包含一系列的值， 因此不能在元组中嵌套另一个元组， 也不能包含由值或元组所组成的列表。 这种简单的数据结构支撑着关系模型 ： 所有操作都必须以元组为目标， 而且其返回值也必须是元组。</p>
<p>面向聚合所用的方式与之不同， 我们通常操作数据时所用的单元， 其结构都比元组集合复杂得多。 如果能够以这种复杂的结构来存放列表或嵌套其他记录结构就好了。大家在后面的章节中将会看到，“键值数据库”、“ 文档数据库”、“ 列族数据库” 都使用这种更为复杂的记录。 然而， 没有公认的术语来称呼这种复杂的记录， 在本书中， 把它叫做“聚合”（ aggregate）。</p>
<p>聚合是“ 领域驱动设计”［ Evans］ 中的术语。 <strong>在领域驱动设计中， 我们想把一组相互关联的对象视为一个整体单元来操作， 而这个单元就叫聚合。</strong> 在涉及数据操作与一致性管理时， 更是如此。 一般情况下， 我们通过原子操作（ atomic operation） 更新聚合的值， 并且在与数据存储通信时， 也以聚合为单位。 这个定义也非常符合“ 键值数据库”、“ 文档数据库” 和“ 列族数据库” 的工作方式。 因为用聚合为单位来复制和分片显得比较自然， 所以在集群中操作数据库时， 还是使用聚合比较简单一些。 此外，由于程序员经常通过聚合结构来操作数据， 故而采用聚合也能让其工作更为轻松。</p>
<h4 id="关系模型与聚合模型示例">关系模型与聚合模型示例</h4><p><img src="/img/2014/QQ图片20150405155439.png" alt=""><br><img src="/img/2014/QQ图片20150405155535.png" alt=""><br>现在我们再来看看， 如果用面向聚合的思路来做， 那么数据模型会是什么样子<br><img src="/img/2014/QQ图片20150405155628.png" alt=""><br>这次也要用一些范例数据， 我们使用 JSON 格式来表示， 因为它是 NoSQL 领域中常用的数据格式。<br><img src="/img/2014/QQ图片20150405155833.png" alt=""></p>
<h4 id="面向聚合的影响">面向聚合的影响</h4><p>关系型数据库的数据模型中， 没有“ 聚合” 这一概念， 因此我们称之为“ 聚合无知”（ aggregate-ignorant）。 NoSQL 领域中的“ 图数据库” 也是聚合无知的。 这一特征并不是坏事。 聚合的边界一般都很难正确划分出来， 当不同场景要使用同一份数据时，更是如此。 </p>
<p>选用面向聚合模型的决定性因素， 就在于它非常适合在集群上运行。 大家应该还记得， 这正是 NoSQL 崛起的杀手锏。在集群上运行时， 我们需要把采集数据时所需的节点数降至最小。如果在数据库中明确包含聚合结构， 那么它就可以根据这一重要信息， 知道哪些数据需要一起操作了， 而且这些数据应该放在同一个节点中。</p>
<p>聚合对于事务处理有一个重要影响。 通常情况下， 面向聚合的数据库确实不支持跨越多个聚合的ACID 事务。 取而代之的是， 它每次只能在一个聚合结构上执行原子操作。 也就是说，如果我们想以原子方式操作多个聚合， 那么就必须自己组织应用程序的代码。</p>
<h3 id="键值数据模型与文档数据模型">键值数据模型与文档数据模型</h3><p>键值数据库的聚合不透明 ， 只包含一些没有太多意义的大块信息 ； 与此相反， 在文档数据库的聚合中， 可以看到其结构。 不透明的优势在于， 聚合中可以存储任意数据。文档数据库则要限制其中存放的内容， 它定义了其允许的结构与数据类型， 而这样做的好处是， 能够更加灵活地访问数据。</p>
<p>在键值数据库中， 要访问聚合内容， 只能通过键来查找。 而使用文档数据库时，则可以用聚合中的字段查询。 我们可以只获取一部分聚合， 而不用获取全部内容， 此外， 数据库还可以按照聚合内容创建索引 。</p>
<h3 id="列族存储">列族存储</h3><p>理解列族模型的最好方式也许就是将其视为两级聚合结构（ two-level aggregate<br>structure）。 与“ 键值存储” 相同，第一个键通常代表行标识符， 可以用它来获取想要的聚合。 列族结构与“ 键值存储” 的区别在于， 其“ 行聚合”（ row aggregate） 本身又是一个映射， 其中包含一些更为详细的值。 这些“ 二级值”（ second-level value）就叫做“ 列”。 与整体访问某行数据一样， 我们也可以操作特定的列。</p>
<p>列族数据库将列组织为列族。 每一列都必须是某个列族的一部分， 而且访问数据的单元也得是列。 这样设计的前提是， 某个列族中的数据经常需要一起访问。<br><img src="/img/2014/QQ图片20150405161008.png" alt=""><br>于是， 我们也得出了两种数据组织方式。</p>
<ul>
<li>面向行（ row-oriented）： 每一行都是一个聚合（ 例如 ID 为 1234 的顾客就是一个聚合）， 该聚合内部存有一些包含有用数据块（ 客户信息、 订单记录） 的列族。</li>
<li>面向列（ column-oriented）： 每个列族都定义了一种记录类型（ 例如客户信息），其中每行都表示一条记录 。 你可以将数据库中的大“ 行” 理解为列族中每一个短行记录的串接。</li>
</ul>
<h3 id="总结">总结</h3><p>键值数据模型将聚合看作不透明的整体，这意味着只能根据键来查出整个聚合， 而不能仅仅查询或获取其中的一部分。</p>
<p>文档模型的聚合对数据库透明， 于是就可以只查询并获取其中一部分数据了， 不过， 由于文档没有模式， 因此在想优化存储并获取聚合中的部分内容时， 数据库不太好调整文档结构。</p>
<p>列族模型把聚合分为列族， 让数据库将其视为行聚合内的一个数据单元。 此类聚合的结构有某种限制， 但是数据库可利用此种结构的优点来提高其易访问性。</p>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="聚合数据模型">聚合数据模型</h2><p><strong>数据模型是认知和操作数据时所用的模型。</strong>对于使用数据库的人来说， 数据模型描述了我们如何同数据库中的数据打交道。 它与存储模型不同， 后者描述了数据库内部存储及操作数据的机制。 </p>
]]>
    </summary>
    
      <category term="nosql" scheme="http://sqtds.github.io/tags/nosql/"/>
    
      <category term="数据库" scheme="http://sqtds.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[MongoDB权威指南笔记]]></title>
    <link href="http://sqtds.github.io/2015/04/05/2014/mongodb-biji/"/>
    <id>http://sqtds.github.io/2015/04/05/2014/mongodb-biji/</id>
    <published>2015-04-05T04:06:03.000Z</published>
    <updated>2015-05-09T04:12:09.000Z</updated>
    <content type="html"><![CDATA[<h2 id="特点">特点</h2><p>MongoDB是它是一个面向集合的,模式自由的文档型数据库。那么什么是文档性数据库呢？它与其他的数据库的差别在哪里？</p>
<ol>
<li><p>面向集合的存储（ Collenction-Orented）：适合存储对象及JSON形式的数据。</p>
<p> 意思是数据被分组存储在数据集中， 被称为一个集合（ Collenction)。每个集合在数据库中都有一个唯一的标识名 ，并且可以包含无限数目的文档 。集合的概念类似关系型数据库（ RDBMS）里的表（ table）， 不同的是它不需要定义任何模式（ schema)。</p>
</li>
<li><p>模式自由（（ schema-free)</p>
<p> 意味着对于存储在 MongoDB 数据库中的文件，我们不需要知道它的任何结构定义。提了这么多次”无模式”或”模式自由 “，它到是个什么概念呢？例如，下面两个记录可以存在于同一个集合里面：<br> {“welcome” : “Beijing”}<br> {“age” : 25}</p>
</li>
<li><p>文档型</p>
<p> 意思是我们存储的数据是键-值对的集合,键是字符串,值可以是数据类型集合里的任意类型,包括数组和文档 . 我们把这个数据格式称作 “ BSON ” 即 “ Binary Serialized dOcument Notation.”</p>
</li>
</ol>
<h2 id="适用场景">适用场景</h2><p><strong>网站数据</strong>： MongoDB 非常适合实时的插入，更新与查询，并具备网站实时数据存储所需的复制及高度伸缩性</p>
<p><strong>缓存</strong>：由于性能很高， MongoDB 也适合作为信息基础设施的缓存层。在系统重启之后，由 MongoDB 搭建的持久化缓存层可以避免下层的数据源过载</p>
<p><strong>大尺寸，低价值的数据</strong>：使用传统的关系型数据库存储一些数据时可能会比较昂贵，在此之前，很多时候程序员往往会选择传统的文件进行存储</p>
<p><strong>高伸缩性的场景</strong>： MongoDB 非常适合由数十或数百台服务器组成的数据库。 MongoDB的路线图中已经包含对 MapReduce 引擎的内置支持</p>
<p><strong>用于对象及 JSON 数据的存储</strong>： MongoDB 的 BSON 数据格式非常适合文档化格式的存储及查询</p>
<h2 id="数据逻辑结构">数据逻辑结构</h2><p> MongoDB 的文档（ document）， 相当于关系数据库中的一行记录。<br> 多个文档组成一个集合（ collection）， 相当于关系数据库的表。<br> 多个集合（ collection）， 逻辑上组织在一起，就是数据库（ database）。<br> 一个 MongoDB 实例支持多个数据库（ database）。<br><img src="/img/2014/mongodb_luojijiegou.png" alt=""><br>文档(document)、集合(collection)、数据库(database)的层次结构如下图:<br><img src="/img/2014/mongodb_chengci.png" alt=""></p>
<h2 id="性能篇">性能篇</h2><p>MongoDB 提供了多样性的索引支持，索引信息被保存在 system.indexes 中，且默认总是为_id<br>创建索引 ，它的索引使用基本和 MySQL 等关系型数据库一样。</p>
<h3 id="基础索引">基础索引</h3><pre><code>在字段 <span class="tag">age</span> 上创建索引， 1(升序);<span class="tag">-1</span>(降序)
<span class="tag">db</span><span class="class">.t3</span><span class="class">.ensureIndex</span>(<span class="rules">{<span class="rule"><span class="attribute">age</span>:<span class="value"><span class="number">1</span>})</span></span></span>
</code></pre><h3 id="文档索引">文档索引</h3><pre><code>db.factories.insert( { name: <span class="string">"wwl"</span>, addr: { city: <span class="string">"Beijing"</span>, <span class="keyword">state</span>: <span class="string">"BJ"</span> } } );
db.factories.ensureIndex( { addr : <span class="number">1</span> } );
//下面这个查询将会用到我们刚刚建立的索引
db.factories.find( { addr: { city: <span class="string">"Beijing"</span>, <span class="keyword">state</span>: <span class="string">"BJ"</span> } } );
//但是下面这个查询将不会用到索引，因为查询的顺序跟索引建立的顺序不一样
db.factories.find( { addr: { <span class="keyword">state</span>: <span class="string">"BJ"</span> , city: <span class="string">"Beijing"</span>} } );
</code></pre><h3 id="组合索引">组合索引</h3><pre><code><span class="keyword">db</span>.factories.ensureIndex( { <span class="string">"addr.city"</span> : 1, <span class="string">"addr.state"</span> : 1 } );
<span class="comment">// 下面的查询都用到了这个索引</span>
<span class="keyword">db</span>.factories.find( { <span class="string">"addr.city"</span> : <span class="string">"Beijing"</span>, <span class="string">"addr.state"</span> : <span class="string">"BJ"</span> } );
<span class="keyword">db</span>.factories.find( { <span class="string">"addr.city"</span> : <span class="string">"Beijing"</span> } );
<span class="keyword">db</span>.factories.find().<span class="keyword">sort</span>( { <span class="string">"addr.city"</span> : 1, <span class="string">"addr.state"</span> : 1 } );
<span class="keyword">db</span>.factories.find().<span class="keyword">sort</span>( { <span class="string">"addr.city"</span> : 1 } )
</code></pre><h2 id="架构篇">架构篇</h2><p>MongoDB 支持在多个机器中通过异步复制达到故障转移和实现冗余。多机器中同一时刻只有一台是用于写操作。正是由于这个情况，为 MongoDB 提供了数据一致性的保障。担当Primary 角色的机器能把读操作分发给 slave。</p>
<h3 id="复制集">复制集</h3><p>MongoDB 高可用可用分两种:</p>
<ul>
<li><p>Master-Slave 主从复制：</p>
<p>  只需要在某一个服务启动时加上–master 参数，而另一个服务加上–slave 与–source 参数，即可实现同步。 MongoDB 的最新版本已不再推荐此方案。</p>
</li>
<li><p>Replica Sets 复制集：</p>
<p>  MongoDB 在 1.6 版本对开发了新功能 replica set，这比之前的 replication 功能要强大一些，增加了故障自动切换和自动修复成员节点，各个 DB 之间数据完全一致，大大降低了维护成功。 auto shard 已经明确说明不支持 replication paris，建议使用 replica set， replica set故障切换完全自动。<br><img src="/img/2014/mongodb_replica.png" alt=""></p>
</li>
</ul>
<h3 id="Sharding_分片">Sharding 分片</h3><p>这是一种将海量的数据水平扩展的数据库集群系统，数据分表存储在 sharding 的各个节点上，使用者通过简单的配置就可以很方便地构建一个分布式 MongoDB 集群。</p>
<p>要构建一个 MongoDB Sharding Cluster，需要三种角色：</p>
<ul>
<li><p>Shard Server</p>
<p>  即存储实际数据的分片，每个 Shard 可以是一个 mongod 实例，也可以是一组 mongod 实例构成的 Replica Set。为了实现每个 Shard 内部的 auto-failover， MongoDB 官方建议每个 Shard为一组 Replica Set。</p>
</li>
<li><p>Config Server</p>
<p>  为了将一个特定的 collection 存储在多个 shard 中，需要为该 collection 指定一个shard key，例如{age: 1} ，shard key 可以决定该条记录属于哪个 chunk。 Config Servers 就是用来存储：所有 shard 节点的配置信息、每个 chunk 的 shard key 范围、 chunk 在各 shard 的分布情况、该集群中所有 DB 和 collection 的 sharding 配置信息。</p>
</li>
<li><p>Route Process</p>
<p>  这是一个前端路由，客户端由此接入，然后询问 Config Servers 需要到哪个 Shard 上查询或保存记录，再连接相应的 Shard 进行操作，最后将结果返回给客户端。客户端只需要将原本发给 mongod 的查询或更新请求原封不动地发给 Routing Process，而不必关心所操作的记录存储在哪个 Shard 上。<br><img src="/img/2014/mongodb_shard.png" alt=""></p>
</li>
</ul>
<h3 id="Replica_Sets_+_Sharding">Replica Sets + Sharding</h3><p>MongoDB Auto-Sharding 解决了海量存储和动态扩容的问题，但离实际生产环境所需的高可<br>靠、高可用还有些距离，所以有了 ” Replica Sets + Sharding”的解决方案:</p>
<ul>
<li><p>Shard:</p>
<p>  使用 Replica Sets，确保每个数据节点都具有备份、自动容错转移、自动恢复能力。</p>
</li>
<li><p>Config:</p>
<p>  使用 3 个配置服务器，确保元数据完整性</p>
</li>
<li><p>Route:</p>
<p>  使用 3 个路由进程，实现负载平衡，提高客户端接入性能</p>
</li>
</ul>
<p><img src="/img/2014/mongodb_replica_shard.png" alt=""></p>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="特点">特点</h2><p>MongoDB是它是一个面向集合的,模式自由的文档型数据库。那么什么是文档性数据库呢？它与其他的数据库的差别在哪里？</p>
<ol>
<li><p>面向集合的存储（ Collenction-Orented）：适合存储对象及JSON形式]]>
    </summary>
    
      <category term="mongodb" scheme="http://sqtds.github.io/tags/mongodb/"/>
    
      <category term="nosql" scheme="http://sqtds.github.io/tags/nosql/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[mondrian 源码解读（九）-聚集层]]></title>
    <link href="http://sqtds.github.io/2015/04/04/2014/mondrian-source-code-9/"/>
    <id>http://sqtds.github.io/2015/04/04/2014/mondrian-source-code-9/</id>
    <published>2015-04-04T10:41:49.000Z</published>
    <updated>2015-05-09T04:12:09.000Z</updated>
    <content type="html"><![CDATA[<p>包mondrian.rolap.agg，管理聚合缓存，这些缓存中包含着各单元格值。<br>RolapStar中含有aggregation(聚合)，一个aggregation是针对一组columns的，该聚合可以包含多个segment，同一个aggregation中的每个segment都将覆盖到相同的列集合；每个segment表达了一组cell值，这些cell自然是由具体的列值和一个必备度量值(如下面的unit sales)来限定，如：<code>(Unit sales, Gender = &#39;F&#39;, State  in {&#39;CA&#39;,&#39;OR&#39;}, Marital Status = anything)</code>，由于其中的列值可能会取多个，因此最终表达的cell值也可能是多个。RolapStar中有一个aggregations，是一个map对象，通过request的constrainedColumnsBitKey来索引一个aggregation。</p>
<h2 id="聚合装载过程">聚合装载过程</h2><p>实际中无论是底层的单元值还是聚合后的单元值都是放在聚合对象aggregation中的。以aggregation.load(colums,measures,predicates,pinnedSegments)为入口：</p>
<p>参数中的除了measure不一样外，其限定的列(colums)及列值（prediactes）都是一致的。因此转换成对对若干segment[]的求值： Segment.load(segment[]，….)，在该方法内部：</p>
<ul>
<li>首先根据segments中的信息生成sql查询语句，有两个不同的生成类：AggQuerySpec和SegmentArrayQuerySpec，前者用于找到聚合表情况下的sql语句生成，后者用于基于原始表的sql语句生成。具体可以参见它们的generateSqlQuery()方法，这里注意对以distinct count有不同的生成方法。Sql生成的核心类是sqlQuery，类似于交换系统中的QuerySqlFactory类。注意：聚合操作如avg、sum等都最终还是利用sql语句实现的，并非mondiran自己实现这些聚合功能。</li>
<li>利用jdbc，执行sql语句，获取到jdbc 结果集。参见mondrian.rolap.RolapUtil.executeQuery()方法。</li>
<li>解析结果集，将结果集中的数据填充到rows[][]二维数值中，并且把各列的值也填充好。如图：<br>结果集每条记录的值如宁波市、G010….,前面两个是维度列值，后面几个是度量值。<br><img src="/img/2014/mondrian_agg1.png" alt=""><br>各列的值(其中第0项值为：[宁波市])：<br><img src="/img/2014/mondrian_agg2.png" alt=""></li>
<li>决定采用稀疏性(sparse)还是稠密性（dense）SegmentDataSet存储（如果是稠密的，就用数组存储，如果是稀疏的，则用Map存储）；并创建该空的DataSet对象。每个segment关联一个DataSet对象；但其稀疏性还是稠密性都是一致的。注意dataset中单元值的个数可能是1个或多个，是由各限定列的指定值个数乘积，若所有限定列都取单值，则显然最终决定一个唯一的单元。</li>
<li>将上述的rows中间集转换到SegmentDataSets集中。最后再分拣给每个segment，确保每个segment的setData(SegmentDataSet)被调用。</li>
</ul>
<h2 id="segment详解">segment详解</h2><p>我们看Segment里面都放了什么：</p>
<pre><code><span class="keyword">protected</span> <span class="keyword">final</span> Column[] columns; <span class="comment">//约束列</span>
<span class="keyword">public</span> <span class="keyword">final</span> MeasureColumn measure;<span class="comment">//这个segment是针对哪个度量的。</span>
<span class="keyword">private</span> RolapModel model; <span class="comment">//对模型的引用</span>
<span class="keyword">public</span> <span class="keyword">final</span> StarColumnPredicate[] predicates; <span class="comment">//Segment存在哪些断言</span>
</code></pre><p>从这里我们可以看出，Segment就是对某个Cube的断面做的定义。那它的单元值是存在哪里呢？<br>就是我们在上面讲的到SegmentDataSet，由它来存储单元值。那么如何关联Segment与SegmentDataSet呢？这里我们有要讲到SegmentWithData。我们看他的定义：</p>
<pre><code>public <span class="class"><span class="keyword">class</span> <span class="title">SegmentWithData</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Segment</span>{</span>
    <span class="keyword">final</span> <span class="type">SegmentAxis</span>[] axes;<span class="comment">//一组维度的约束</span>
    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">SegmentDataset</span> data;<span class="comment">//数据存储</span>
...
    <span class="comment">//此方法判断SegmentWithData中是否存在以keys为维度的单元值</span>
    public <span class="type">Object</span> getCellValue(<span class="type">Object</span>[] keys) {
        assert keys.length == axes.length;
        int missed = <span class="number">0</span>;
        <span class="type">CellKey</span> cellKey = <span class="type">CellKey</span>.<span class="type">Generator</span>.newCellKey(axes.length);
        <span class="keyword">for</span> (int i = <span class="number">0</span>; i &lt; keys.length; i++) {
            <span class="type">Comparable</span> key = (<span class="type">Comparable</span>) keys[i];
            int offset = axes[i].getOffset(key);
            <span class="keyword">if</span> (offset &lt; <span class="number">0</span>) {
                <span class="keyword">if</span> (axes[i].wouldContain(key)) {
                    <span class="comment">// see whether this segment should contain this value</span>
                    missed++;
                    <span class="keyword">continue</span>;
                } <span class="keyword">else</span> {
                    <span class="comment">// this value should not appear in this segment; we</span>
                    <span class="comment">// should be looking in a different segment</span>
                    <span class="keyword">return</span> <span class="literal">null</span>;
                }
            }
            cellKey.setAxis(i, offset);
        }
        <span class="keyword">if</span> (isExcluded(keys)) {
            <span class="comment">// this value should not appear in this segment; we</span>
            <span class="comment">// should be looking in a different segment</span>
            <span class="keyword">return</span> <span class="literal">null</span>;
        }
        <span class="keyword">if</span> (missed &gt; <span class="number">0</span>) {
            <span class="comment">// the value should be in this segment, but isn't, because one</span>
            <span class="comment">// or more of its keys does have any values</span>
            <span class="keyword">return</span> <span class="type">FunUtil</span>.nullValue;
        } <span class="keyword">else</span> {
            <span class="comment">//cellkey是对一组维度的值得定义</span>
            <span class="type">Object</span> o = data.getObject(cellKey);
            <span class="keyword">if</span> (o == <span class="literal">null</span>) {
                o = <span class="type">FunUtil</span>.nullValue;
            }
            <span class="keyword">return</span> o;
        }
    }
}    
</code></pre><p>在这里，SegmentWithData包装了Segment与它单元值之间的关系，通过SegmentAxis来判断当前比较的值是否相同。</p>
<p>下图是有两个限定列的两个segment的描述(注:其中roadid列虽然指定了8个候选值，但由于使用了空行/列过滤，最后只剩下两个路线有值，故最后segment结果集的单元数也只有两个，对应于G010和G318的)：<br><img src="/img/2014/mondrian_agg3.png" alt=""><br><img src="/img/2014/mondrian_agg4.png" alt=""><br>其中第二个对应的dataset为：<br>[317.769, 120.604]<br>对应的透视界面为(参见其中的“观测里程”度量值，与上面的dataset一致)：<br><img src="/img/2014/mondrian_agg5.png" alt=""></p>
<p>再譬如有三个限定列的segment描述，它们位于另一个aggreation对象中：(其中timeId列的any代表所以可能的时间值，共有2003~2005三个年，所以最终该segment共有3个cell值)<br><img src="/img/2014/mondrian_agg6.png" alt=""><br>对应的dataset为：<br>[129.910, 129.909, 57.950]<br>对应的透视界面为(显然该aggreation还有另外一个segment，其中的roadId对应于G010—宁波梁辉)：<br><img src="/img/2014/mondrian_agg7.png" alt=""></p>
<p>再譬如维度中有多个层次的情况时，一个维度会对应多个列：</p>
<pre><code><span class="type">Query</span> query = connection.parseQuery(
    <span class="string">"SELECT"</span> +
    <span class="string">" {[Time].[1997],"</span> +
    <span class="string">" [Time].[1997].Children} ON COLUMNS,"</span> +
    <span class="string">" {[Customer].[USA],"</span> +
    <span class="string">" [Customer].[USA].[OR],"</span> +
    <span class="string">" [Customer].[USA].[WA]} ON ROWS"</span> +
    <span class="string">"FROM [Sales]"</span>);
<span class="type">Result</span> <span class="literal">result</span> = connection.execute(query);
</code></pre><p>该语句执行后产生的segment分别为(除了第一个外，其他segment都会包含多个cell，因为它们的限定列中含有多值的情况)：</p>
<pre><code>Segment YN<span class="comment">#1    Year Nation Unit Sales</span>
                <span class="number">1997</span> USA    xxx
Predicates: <span class="variable">Year=</span><span class="number">1997</span>, <span class="variable">Nation=</span>USA

Segment YNS<span class="comment">#1    Year Nation State Unit Sales</span>
                <span class="number">1997</span> USA    OR    xxx
                <span class="number">1997</span> USA    WA    xxx
Predicates: <span class="variable">Year=</span><span class="number">1997</span>, <span class="variable">Nation=</span>USA, <span class="variable">State=</span>{OR, WA}

Segment YQN<span class="comment">#1    Year Quarter Nation Unit Sales</span>
                <span class="number">1997</span> Q1      USA    xxx
                <span class="number">1997</span> Q2      USA    xxx
Predicates: <span class="variable">Year=</span><span class="number">1997</span>, <span class="variable">Quarter=</span>any, <span class="variable">Nation=</span>USA

Segment YQNS<span class="comment">#1    Year Quarter Nation State Unit Sales</span>
                <span class="number">1997</span> Q1      USA    OR    xxx
                <span class="number">1997</span> Q1      USA    WA    xxx
                <span class="number">1997</span> Q2      USA    OR    xxx
                <span class="number">1997</span> Q2      USA    WA    xxx
Predicates: <span class="variable">Year=</span><span class="number">1997</span>, <span class="variable">Quarter=</span>any, <span class="variable">Nation=</span>USA, <span class="variable">State=</span>{OR, WA}
</code></pre>]]></content>
    <summary type="html">
    <![CDATA[<p>包mondrian.rolap.agg，管理聚合缓存，这些缓存中包含着各单元格值。<br>RolapStar中含有aggregation(聚合)，一个aggregation是针对一组columns的，该聚合可以包含多个segment，同一个aggregation中的每个se]]>
    </summary>
    
      <category term="mondrian" scheme="http://sqtds.github.io/tags/mondrian/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[mondrian 源码解读（八）-计算结果]]></title>
    <link href="http://sqtds.github.io/2015/04/04/2014/mondrian-source-code-8/"/>
    <id>http://sqtds.github.io/2015/04/04/2014/mondrian-source-code-8/</id>
    <published>2015-04-04T10:07:17.000Z</published>
    <updated>2015-05-09T04:12:09.000Z</updated>
    <content type="html"><![CDATA[<p>上一节我们了解了member的查询过程。下面，我们将进入mdx的执行过程。</p>
<p>首先，我们需要了解3个重要的类，结果集RolapResult，求值上下文RolapEvaluator与单元格读取CellReader。这3个类的关系是首先由在RolapResult初始化构造函数中传入一个Query，由Query生成一个RolapEvaluator，然后由Query中的Calc根据RolapEvaluator来获取轴上的成员。获取到所有的成员之后，RolapEvaluator根据成员调用CellReader获取到当前值。</p>
<h2 id="RolapResult">RolapResult</h2><p>RolapResult是一个运行中的请求的结果集。</p>
<p>Mondiran的执行结果由RolapResult类表单，由于mdx查询语句本身就包含on rows(行轴上)、on columns（列轴上）和where部分（切片轴上），结果集中相对应的为ROlapAxis对象，这其中有个sliceAxis对象。因此结果集是由若干ROlapAxis对象和一个RolapCell组构成的。每个axis对象又由若干Position对象组成，每个Position对象又可能由若干member组成（注意一个postion会横跨多个维度的成员）。注意ROlapAxis是抽象类，实际的对象类可能随着不同的轴是不同的。如图：<br><img src="/img/2014/mondrian_result.png" alt=""></p>
<p>图中，column轴上两个position(每个position含有一个成员)，分别是:</p>
<pre><code> [[Measures].[YJD]]
[[Measures].[GCLC]]
</code></pre><p>Row轴上有三个position(每个position含有二个成员)，分别是:</p>
<pre><code>[[dimLX].[All dimLXs], [dimTime].[All dimTimes]]
[[dimLX].[All dimLXs].[宁波—梁辉], [dimTime].[All dimTimes]]
[[dimLX].[All dimLXs].[同江－三亚], [dimTime].[All dimTimes]]
</code></pre><p>切片轴上则有一个position：</p>
<pre><code>[[dimStation].[All dimStations].[宁波市]]
</code></pre><p>单元值们则放置在RolapResult中的cellInfos对象里，属CellInfoContainer接口，其中存放着CellInfo，并通过Cellkey进行索引。</p>
<p>CellKey：用于在maps里访问cellinfo时使用的键值，根据cell的位置来决定键值。CellKey共有四个默认实现，及zero、one、two、three和many版的实现，分别对应着轴的个数。这些类中关键的属性便是存储各轴的位置值。</p>
<p>CellInfo、CellInfoContainer：内部类。CellInfo包含了一个cell所需要的所有信息(最关键的包含value值和一些formatter设置);最终将作为构造ROlapCell对象的参数。CellInfoContainer显然是cellInfo的容器，并使用CellKey来索引。</p>
<p>ROlapCell：最终返回给jpivot的cell单元值。</p>
<h2 id="RolapEvaluator">RolapEvaluator</h2><p>RolapEvaluator即在多维环境中计算表达式。<br>该类中维护一个很重要的对象，即currentMembers,该上下文对象针对每个维度都包含了一个成员；通过setContext方法用来设置当前维度，以开始计算当前维度组合下的表达式值。</p>
<p>该类有一个方法：<br><code>public final Object evaluateCurrent()</code><br>该方法就是对单元格的求值，在该方法中，规定了solver order的求解顺序。</p>
<h2 id="CellReader">CellReader</h2><p>CellReader即单元格读取。</p>
<p>Cells会被求值多次。第一次时, Evaluator使用FastBatchingCellReader来求值。当一个单元被求值时，evaluateCurrent()被调用。此时FastBatchingCellReader并没有被调用，而是为那个cell记录了一个 CellRequest并且return (not throw) an exception。在所有的cells都有了对应的CellRequests之后， Aggregation会生成 SQL，以一个单独的sql请求来载入所有的cells。然后由AggregatingCellReader 重新计算cells，从缓存中返回cells值。<br><img src="/img/2014/CellReader.gif" alt=""></p>
<h3 id="FastBatchingCellReader">FastBatchingCellReader</h3><p>主要方法，Object get(Evaluator  evaluator)</p>
<ol>
<li>首先根据当前的上下文环境（即一组members）创建cellRequest，cellRequest中包含了所有必要的从star中取值的信息。该组members的交集便是要求值的单元格，其中切片轴上的成员和其他轴上的成员完全同等对待；其中度量轴上的成员要求上StoredMeasure（非计算成员CaculatedMember）；度量值上的成员位于第一个。通过调用request的addConstrainedColumn()方法把各member对应的column和value（属StarColumnPredicate）值加至到request中.</li>
<li>调用AggregationManager.getCellFromCache(request,pinnedSegments)方法从缓存中获取cell值。首先根据request中的列组索引标识从缓存中获取aggreation缓存对象，如果为空说明缓存还未建立则直接返回null，如果有值则调用aggregation.getCellValue(measure,colValueKeys)方法获取缓存的cell值；getCellValue内部首先会根据measure查找匹配的segment，然后调用segment.getCellValue(keys)从segment的dataset缓存集中查找相应的cell值。</li>
<li>如果getCellFromCache返回为null则调用recordCellRequest（）记录需求。这些cell request会被组织成多个cell request batch，以便将来聚合层进行批读取以提高效率。关于batch的详细讨论参见下面Batch类章节。</li>
<li>上层会在适当的时候调用batchCellReading.loadAggregations()以实际读取这些cell值，前提是batches对象中已有cellRequest了。每个batch的读取参见batch. loadAggregation()方法，最终调用聚合层的方法，参见aggreation.load(….)。</li>
</ol>
<h3 id="FastBatchingCellReader-Batch类">FastBatchingCellReader.Batch类</h3><p>每个batch对应与一组特定的columns环境下的cell求取（具有相同的列和列值(列值是具体的值，不会是“all”值)）；从batch的属性可以看出batch包含了哪些上下文：</p>
<ol>
<li>RolapStar.Column[]，这个指明了基于哪些列(也即基于哪些维度，包括切片维度)进行读取；</li>
<li>Set<starcolumnpredicate>[]，保存了每列的限定值，对于一列而言，限定值可能会有多个（毕竟是批处理，一次请求多个）；</starcolumnpredicate></li>
<li>MeasureList，指明求取哪些度量值上的cell（度量值本质是度量维上的限定值）。</li>
<li>BitKey，该batch的唯一索引。<br>如图所示的一个mdx查询结果界面：<br><img src="/img/2014/fasterBatch.png" alt=""></li>
</ol>
<p>此时会产生两个batch，每个batch最终可能会产生若干segment，segment是cells的集合，</p>
<ol>
<li><p>一个batch是(其中“当量数/适应交通量=拥挤度”，拥挤度是计算成员)，最终产生3个segment，每个segment只有一个cell：</p>
<p>  （地市=’宁波市’,measure=’观察里程’）    </p>
<pre><code><span class="attribute">（地市</span>=<span class="string">’宁波市’,measure=’当量数’）</span>
</code></pre><p>  （地市=’宁波市’,measure=’适应交通量’）</p>
</li>
<li><p>另一个batch是(其中的G310等是路线代码，最终过滤掉空值后就剩下两个了) ，最终产生3个segment，每个segment有多个cell：</p>
<pre><code>（地市=’宁波市’,roadId <span class="keyword">in</span> (G310,G322,G210,S321….)，<span class="variable">measure=</span>’观察里程’）
（地市=’宁波市’,roadId <span class="keyword">in</span> (G310,G322,G210,S321….)，<span class="variable">measure=</span>’当量数’） 
（地市=’宁波市’,roadId <span class="keyword">in</span> (G310,G322,G210,S321….)，<span class="variable">measure=</span>’适应交通量’）
</code></pre></li>
</ol>
<p>此次：mdx的执行过程就分析完了。<br>ps：最后这一点来源于网络，由于从文档上无法得知作者来源，如有侵权还望见谅。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>上一节我们了解了member的查询过程。下面，我们将进入mdx的执行过程。</p>
<p>首先，我们需要了解3个重要的类，结果集RolapResult，求值上下文RolapEvaluator与单元格读取CellReader。这3个类的关系是首先由在RolapResult初始]]>
    </summary>
    
      <category term="mondrian" scheme="http://sqtds.github.io/tags/mondrian/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Oracle rollup、cube、grouping sets]]></title>
    <link href="http://sqtds.github.io/2015/03/31/2014/oracle-group-by/"/>
    <id>http://sqtds.github.io/2015/03/31/2014/oracle-group-by/</id>
    <published>2015-03-31T09:45:27.000Z</published>
    <updated>2015-04-04T10:10:45.000Z</updated>
    <content type="html"><![CDATA[<p>Oracle的group by除了基本用法以外，还有3种扩展用法，分别是rollup、cube、grouping sets。</p>
<h2 id="1_rollup">1 rollup</h2><p>假设有一个表test，有A、B、C、D、E5列。<br>如果使用group by rollup(A,B,C)，首先会对(A、B、C)进行GROUP BY，然后对(A、B)进行GROUP BY，然后是(A)进行GROUP BY，最后对全表进行GROUP BY操作。roll up的意思是“卷起”，这也可以帮助我们理解group by rollup就是对选择的列从右到左以一次少一列的方式进行grouping直到所有列都去掉后的grouping(也就是全表grouping)，对于n个参数的rollup，有n+1次的grouping。以下2个sql的结果集是一样的：</p>
<p><code>Select A,B,C,sum(E) from test group by rollup(A,B,C)</code><br>与</p>
<p><code>Select A,B,C,sum(E) from test group by A,B,C
union all
Select A,B,null,sum(E) from test group by A,B
union all
Select A,null,null,sum(E) from test group by A
union all
Select null,null,null,sum(E) from test</code></p>
<h2 id="2_cube">2 cube</h2><p>cube的意思是立方，对cube的每个参数，都可以理解为取值为参与grouping和不参与grouping两个值的一个维度，然后所有维度取值组合的集合就是grouping的集合，对于n个参数的cube，有2^n次的grouping。如果使用group by cube(A,B,C),，则首先会对(A、B、C)进行GROUP BY，然后依次是(A、B)，(A、C)，(A)，(B、C)，(B)，(C)，最后对全表进行GROUP BY操作，一共是2^3=8次grouping。同rollup一样，也可以用基本的group by加上结果集的union all写出一个与group by cube结果集相同的sql：<br><code>Select A,B,C,sum(E) from test group by cube(A,B,C);</code><br>与<br><code>Select A,B,C,sum(E) from test group by A,B,C
union all
Select A,B,null,sum(E) from test group by A,B
union all
Select A,null,C,sum(E) from test group by A,C
union all
Select A,null,null,sum(E) from test group by A
union all
Select null,B,C,sum(E) from test group by B,C
union all
Select null,B,null,sum(E) from test group by B
union all
Select null,null,C,sum(E) from test group by C
union all
Select null,null,null,sum(E) from test;</code></p>
<h2 id="3_grouping_sets">3 grouping sets</h2><p>grouping sets就是对参数中的每个参数做grouping，也就是有几个参数做几次grouping,例如使用<code>group by grouping sets(A,B,C)</code>，则对(A),(B),(C)进行group by，如果使用<code>group by grouping sets((A,B),C)</code>,则对(A,B),(C)进行group by。甚至<code>grouping by grouping set(A,A)</code>都是语法允许的，也就是对(A)进行2次group by,grouping sets的参数允许重复</p>
<h2 id="4_总结">4 总结</h2><ul>
<li>rollup        (N+1个分组方案)</li>
<li>cube         (2^N个分组方案)</li>
<li>grouping sets (自定义罗列出分组方案)</li>
</ul>
<hr>
<h2 id="5_注意点">5 注意点</h2><h3 id="5-1_机制不同">5.1 机制不同</h3><p>在rollup和cube的说明中分别给出了用基本group by加结果集union all给出了结果集相同的sql，但这只是为了理解的方便而给出的sql，并不说明rollup和cube与基本group by加结果集union all等价。实际上两者的内部机制是安全不一样的，前者除了写法简洁以外，运行时不需多次扫描表，效率远比后者高。</p>
<h3 id="5-2_集合可运算">5.2 集合可运算</h3><p>3种扩展用法的参数可以是源表中的某一个具体的列，也可以是若干列经过计算而形成的一个新列（比如说A+B，A||B），也可以是这两种列的一个集合（例如（A+B，C）），对于grouping set更是特殊，可以是空集合()，表示对全表进行group by。</p>
<h3 id="5-3_group_by_与_rollup,_cube组合使用">5.3 group by 与 rollup, cube组合使用</h3><p>Group by的基本用法以及这3种扩展用法可以组合使用，也就是说可以出现group by A,rollup(A,B)这样的用法，oracle将对出现在group by中的每种用法的grouping列集合做笛卡尔积然后对其中的每一个元素做group by。这话说起来挺绕口，举例说明吧，group by A, rollup(A,B)，基本用法的grouping集合是(A),rollup(A,B)的grouping集合是((A,B),(A),()),两个集合的笛卡尔积集合是((A,A,B),(A,A),(A))，所以会首先对(A,A,B)做group by，然后对(A,A)做group by，最后对(A)做group by。实际上对(A,A,B)做group by和对(A,B)做group by两者是完全等价的(group by A,A,B结果和group by A,B完全一样)，同理对(A,A)做group by和对(A)做group by也是等价的。简化后的结果就是首先对(A,B)做group by，然后对(A)做group by，最后再对(A)做group by。下面给出两个等价的sql以便理解：<br><code>Select A,B,sum(E) from test1 group by A, rollup(A,B);</code><br>与<br><code>Select A,B,sum(E) from test1 group by A,B
Union all
Select A,null,sum(E) from test1 group by A
Union all
Select A,null,sum(E) from test1 group by A;</code></p>
<h2 id="6_grouping()、grouping_id()、group_id()">6 grouping()、grouping_id()、group_id()</h2><h3 id="6-1_grouping()">6.1 grouping()</h3><p>参数只有一个，而且必须为group by中出现的某一列，表示结果集的一行是否对该列做了grouping。对于对该列做了grouping的行而言，grouping()=0，反之为1；</p>
<h3 id="6-2_grouping_id()">6.2 grouping_id()</h3><p>参数可以是多个，但必须为group by中出现的列。Grouping_id()的返回值其实就是参数中的每列的grouping()值的二进制向量，例如如果grouping(A)=1，grouping(B)=0，则grouping_id(A,B)的返回值就是二进制的10，转成10进制就是2。</p>
<h3 id="6-3_group_id()">6.3 group_id()</h3><p>无参数。见上面的说明3），group by对某些列的集合会进行重复的grouping，而实际上绝大多数情况下对结果集中的这些重复行是不需要的，那就必须有办法剔出这些重复grouping的行。当结果集中有n条重复grouping而形成的行时，每行的group_id()分别是0,1,…,n，这样我们在条件中加入一个group_id()&lt;1就可以剔出这些重复grouping的行了。</p>
<h2 id="7_示例">7 示例</h2><h3 id="7-1_建表与数据">7.1 建表与数据</h3><p>SQL&gt; <code>create table test(department_id number, a varchar2(20), b varchar2(20));</code></p>
<p>Table created</p>
<p>SQL&gt; <code>insert into test values(10, &#39;A&#39;, &#39;B&#39;);</code></p>
<p>1 row inserted</p>
<p>SQL&gt; <code>commit;</code></p>
<p>Commit complete</p>
<h3 id="7-2_查询语句">7.2 查询语句</h3><p><code>select department_id,
       a,
       b,
       grouping(department_id),
       grouping(a),
       grouping(b)
  from test
 group by rollup(department_id, a, b)
order by 4, 5, 6;
select department_id,
       a,
       b,
       grouping(department_id),
       grouping(a),
       grouping(b)
  from test
 group by cube(department_id, a, b)
order by 4, 5, 6;</code></p>
]]></content>
    <summary type="html">
    <![CDATA[<p>Oracle的group by除了基本用法以外，还有3种扩展用法，分别是rollup、cube、grouping sets。</p>
<h2 id="1_rollup">1 rollup</h2><p>假设有一个表test，有A、B、C、D、E5列。<br>如果使用grou]]>
    </summary>
    
      <category term="groupby" scheme="http://sqtds.github.io/tags/groupby/"/>
    
      <category term="oralce" scheme="http://sqtds.github.io/tags/oralce/"/>
    
      <category term="数据库" scheme="http://sqtds.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[mondrian 源码解读（番外篇）-MDX函数的执行过程详解]]></title>
    <link href="http://sqtds.github.io/2014/11/13/2014/mondrian-ext-function%E6%89%A7%E8%A1%8C%E6%9C%BA%E5%88%B6/"/>
    <id>http://sqtds.github.io/2014/11/13/2014/mondrian-ext-function执行机制/</id>
    <published>2014-11-13T10:07:10.000Z</published>
    <updated>2015-04-04T09:09:12.000Z</updated>
    <content type="html"><![CDATA[<h2 id="MDX函数的执行过程详解">MDX函数的执行过程详解</h2><p>笔者注：由于将来公司可能会有对函数的二次开发需求，所以为公司写了函数的执行过程，以供将来使用。</p>
<h3 id="一，Query对象的创建">一，Query对象的创建</h3><p>在Query对象中，最主要的是resolve()方法，这里涉及到了2个非常重要的功能。</p>
<ul>
<li><p>第一，轴（查询轴和切片轴）上表达式(Exp)的转换。</p>
<p>  主要是将表达式从UnresolvedFunCall转换为ResolvedFunCall。</p>
</li>
<li><p>第二，轴上的计算器(Calc)的创建。</p>
<p>  主要是针对各个轴如何创建计算器。</p>
</li>
</ul>
<p>下面是的resolve()方法解释：</p>
<pre><code> <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">resolve</span><span class="params">()</span> </span>{
    <span class="keyword">final</span> Validator validator = createValidator(); <span class="comment">//1</span>
    resolve(validator); <span class="comment">// resolve self and children ,2</span>
    <span class="comment">// Create a dummy result so we can use its evaluator</span>
    <span class="keyword">final</span> Evaluator evaluator = Util.createEvaluator(<span class="keyword">this</span>); <span class="comment">//3</span>
    ExpCompiler compiler =
            createCompiler(
                    evaluator, validator, Collections.singletonList(resultStyle)); <span class="comment">//4</span>
    compile(compiler);  <span class="comment">//5</span>
}
</code></pre><ol>
<li>第1行创建校验器(Validator)。</li>
<li>第2行根据Validator转换所有的UnresolvedFunCall到ResolvedFunCall。</li>
<li>第3行针对当前Query创建求值器(Evaluator)。</li>
<li>第4行根据求值器，校验器创建表达式编译器(ExpCompiler)。</li>
<li>第5行根据表达式编译器编译所有的轴，并创建轴上的计算器。</li>
</ol>
<a id="more"></a>
<p>首先我们看Validator接口：</p>
<pre><code><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Validator</span> </span>{
    <span class="function">Exp  <span class="title">validate</span><span class="params">(Exp exp, <span class="keyword">boolean</span> scalar)</span></span>;
    <span class="function"><span class="keyword">void</span> <span class="title">validate</span><span class="params">(QueryAxis axis)</span></span>;
    <span class="function"><span class="keyword">void</span> <span class="title">validate</span><span class="params">(Formula formula)</span></span>;
    ...
}
</code></pre><p>很显然，这是一个访问者模式，针对不同的对象进行不同的操作。看完Validator接口后，我们看<code>resolve()</code>方法的时序图，如下：</p>
<p><img src="/img/query.png" alt="时序图"></p>
<p>其他功能例如表达式的查找，度量成员的查找,公式(formulas)的成员（member）或者集（set）的创建创建等也是在这个过程中执行的，这里就不一一介绍了。下面将对主要的2点做详细的介绍。</p>
<hr>
<h4 id="1-1轴上表达式(Exp)的转换">1.1轴上表达式(Exp)的转换</h4><p>我们看上图，当执行Query的<code>validate(axis)</code>方法时：</p>
<ol>
<li>QueryAxis会调用Validator的<code>void validate(QueryAxis axis)</code>方法</li>
<li>Validator反过来调用QueryAxis的<code>public void resolve(Validator validator)</code>方法</li>
<li>QueryAxis又会调用Validator的<code>public Exp validate(Exp exp, boolean scalar)</code>方法</li>
<li>接着Validator又会调用UnresolvedFunCall的<code>public Exp accept(Validator validator)</code>方法。</li>
<li>最后UnresolvedFunCall的<code>accept(Validator validator)</code>会创建一个ResolvedFunCall对象返回到QueryAxis的exp上。</li>
</ol>
<p>现在我们看UnresolvedFunCall的<code>accept(Validator validator)</code>方法，看它是如何执行的。</p>
<pre><code><span class="comment">//UnresolvedFunCall</span>
<span class="keyword">public</span> <span class="function">Exp <span class="title">accept</span><span class="params">(Validator validator)</span> </span>{
    Exp[] newArgs = <span class="keyword">new</span> Exp[args.length]; <span class="comment">//1</span>
    FunDef funDef =
            FunUtil.resolveFunArgs(
                    validator, <span class="keyword">null</span>, args, newArgs, name, syntax);  <span class="comment">//2</span>
    <span class="function"><span class="keyword">return</span> funDef.<span class="title">createCall</span><span class="params">(validator, newArgs)</span></span>;  <span class="comment">//3</span>
}
</code></pre><p>它的执行有一下几步：</p>
<ol>
<li>创建表达式参数</li>
<li>解析表达式为指定的函数定义(FunDef)</li>
<li>创建已解析的函数调用(ResolvedFunCall)</li>
</ol>
<p>这里有2个点需要关注，第2和第3。我们看看它是如何执行的。</p>
<hr>
<h5 id="1-1-1解析表达式为指定的函数定义(FunDef)">1.1.1解析表达式为指定的函数定义(FunDef)</h5><p>下面是FunUtil的resolveFunArgs方法。</p>
<pre><code><span class="comment">//FunUtil</span>
<span class="keyword">public</span> <span class="keyword">static</span> <span class="function">FunDef <span class="title">resolveFunArgs</span><span class="params">(
        Validator validator,
        FunDef funDef,
        Exp[] args,
        Exp[] newArgs,
        String name,
        Syntax syntax)</span>
</span>{
    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; args.length; i++) {
        newArgs[i] = validator.validate(args[i], <span class="keyword">false</span>);  <span class="comment">//2</span>
    }
    <span class="keyword">if</span> (funDef == <span class="keyword">null</span> || validator.alwaysResolveFunDef()) {
        funDef = validator.getDef(newArgs, name, syntax);  <span class="comment">//5</span>
    }
    <span class="keyword">return</span> funDef;
}
</code></pre><p>我们看第2行<code>newArgs[i] = validator.validate(args[i], false)</code>，这一行对参数进行了解析，最后返回解析后的表达式赋值给新参数。注意这里的参数可以是维度，成员，ID，函数或者其他的表达式。<br>第5行 <code>funDef = validator.getDef(newArgs, name, syntax)</code>，即通过新参数，表达式名称，和语义(Syntax)来构建一个FunDef，最后返回这个FunDef。</p>
<p>下面，我们先要了解什么是函数定义(FunDef)。看一下它的接口：</p>
<pre><code><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title">FunDef</span> {

    <span class="function">Syntax <span class="title">getSyntax</span><span class="params">()</span></span>; <span class="comment">//函数语义，例如函数，前缀，中缀，后缀等。</span>

    <span class="function">String <span class="title">getName</span><span class="params">()</span></span>;  <span class="comment">//函数名称</span>

    <span class="function"><span class="keyword">int</span> <span class="title">getReturnCategory</span><span class="params">()</span></span>;  <span class="comment">//返回类型</span>

    <span class="keyword">int</span>[] getParameterCategories();  <span class="comment">//参数类型</span>

    <span class="function">Calc <span class="title">compileCall</span><span class="params">(ResolvedFunCall call, ExpCompiler compiler)</span></span>;    <span class="comment">//创建求值器</span>

    <span class="function">Exp <span class="title">createCall</span><span class="params">(Validator validator, Exp[] args)</span></span>;    <span class="comment">//创建表达式，一般来说是ResolvedFunCall</span>

}
</code></pre><p>很明显，FunDef描述了函数的特征：语义，名称，返回类型，参数类型。还有2个接口，compileCall定义如何创建求值器，createCall定义了如何创建表达式。</p>
<p>了解清楚了FunDef后，我们再看如何获取FunDef，即第5行的<code>validator.getDef(newArgs, name, syntax)</code>。我们把主干抽出来，代码如下：</p>
<pre><code><span class="comment">//Validator实现</span>
<span class="keyword">public</span> <span class="function">FunDef <span class="title">getDef</span><span class="params">(
    Exp[] args,
    String funName,
    Syntax syntax)</span>
</span>{
    <span class="comment">//...</span>

    List&lt;Resolver&gt; resolvers = funTable.getResolvers(funName, syntax);  <span class="comment">//1</span>

    <span class="keyword">final</span> List&lt;Resolver.Conversion&gt; conversionList =
        <span class="keyword">new</span> ArrayList&lt;Resolver.Conversion&gt;();
    List&lt;FunDef&gt; matchDefs = <span class="keyword">new</span> ArrayList&lt;FunDef&gt;();
    <span class="keyword">for</span> (Resolver resolver : resolvers) {
        FunDef def = resolver.resolve(args, <span class="keyword">this</span>, conversionList); <span class="comment">//2</span>
        <span class="keyword">if</span> (def != <span class="keyword">null</span>) {
            <span class="comment">//....</span>
            matchDefs.add(def);
            <span class="comment">//...</span>
        }
    }

    <span class="keyword">switch</span> (matchDefs.size()) {
    <span class="keyword">case</span> <span class="number">0</span>:
        <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"No Function Matches Signature&gt;&gt;&gt;"</span>+
            signature);
    <span class="keyword">case</span> <span class="number">1</span>:
        <span class="keyword">break</span>;
    <span class="keyword">default</span>:
        <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"MoreThanOneFunctionMatchesSignature"</span>+
                signature+
                buf.toString());
    }

    <span class="comment">// ...</span>

    <span class="keyword">return</span> matchDef;
}
</code></pre><p>这里首先做的是funTable通过名称(funName)和语义(syntax)来获取解析器(Resolver)列表，这应该是这里最重要的一部分了。然后再根据获取到的解析器列表来解析参数（args）获取到具体的FunDef。这刚好与FunDef的描述相符合，即语义，名称，参数类型决定了某个具体的函数。注意，要理解的是FunDef还有个返回类型，为什么这里不需要呢这？就像java的方法一样，方法可以同名，不同的参数，单返回类型必须只有一种，不可能定义出重载返回类型的方法出来。</p>
<p>所以了解了这些以后，我们继续看第1处的 <code>funTable.getResolvers(funName, syntax);</code>和第2处的<code>resolver.resolve(args, this, conversionList)</code>就很简单了。这里不就详细解释了，详情请看如何定义一个函数部分。</p>
<h5 id="1-1-2创建已解析的函数调用(ResolvedFunCall)">1.1.2创建已解析的函数调用(ResolvedFunCall)</h5><p>创建完FunDef后，我们再回到UnresolvedFunCall的<code>accept(Validator validator)</code>方法。<br>我们看ResolvedFunCall是如何被创建的。由于大部分FunDef继承的FunDefBase，我们只需要看FunDefBase中的<code>createCall(Validator validator, Exp[] args)</code>方法就可以了。</p>
<pre><code><span class="comment">//FunDefBase</span>
public <span class="type">Exp</span> createCall(<span class="type">Validator</span> validator, <span class="type">Exp</span>[] args) {
    int[] categories = getParameterCategories();  <span class="comment">//1</span>
    <span class="keyword">final</span> <span class="type">Type</span> <span class="class"><span class="keyword">type</span> =</span> getResultType(validator, args);  <span class="comment">//2</span>
    <span class="keyword">if</span> (<span class="class"><span class="keyword">type</span> <span class="title">==</span> <span class="title">null</span>) {</span>
        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="type">RuntimeException</span>(<span class="string">"could not derive type"</span>);
    }
    <span class="keyword">return</span> <span class="keyword">new</span> <span class="type">ResolvedFunCall</span>(<span class="keyword">this</span>, args, <span class="class"><span class="keyword">type</span>);</span>  <span class="comment">//3</span>
}
</code></pre><ol>
<li>第一步获取参数类型</li>
<li>第二步获取返回类型，这里要注意的是，对于(cube,dimension, hierarchy, level, member)的返回类型，我们需要指定到某个具体的返回类型中。假设返回一个level类型，我们要知道这个level是属于哪个hierarchy，哪个dimension。所以FunBase默认情况下是取的第一个参数的返回类型进行填充。对于某些函数来说，并不是第一个参数决定返回类型，所以需要自己实现。</li>
<li>根据this，args，type创建ResolvedFunCall。</li>
</ol>
<p>最后将解析的ResolvedFunCall返回给QueryAxis，这样，轴（查询轴和切片轴）上表达式(Exp)的转换就完成了。</p>
<h4 id="1-2轴上的计算器(Calc)的创建">1.2轴上的计算器(Calc)的创建</h4><p>跳到最上面，接下来我们看如何创建轴上的计算器(Calc）。<br>第3,4,5步是我们比较关心的。第3,4步比较简单，这里就不展开了。<br>我们看第5步，在<code>compile(compiler)</code>方法内部，调用了轴上的<code>compile(ExpCompiler compiler, ResultStyle resultStyle)</code>方法。我们看方法的具体实现：</p>
<pre><code><span class="function"><span class="keyword">public</span> Calc <span class="title">compile</span><span class="params">(ExpCompiler compiler, ResultStyle resultStyle)</span> </span>{
    Exp <span class="built_in">exp</span> = <span class="keyword">this</span>.<span class="built_in">exp</span>;
    <span class="keyword">if</span> (axisOrdinal.isFilter()) {              <span class="comment">//1</span>
        <span class="built_in">exp</span> = normalizeSlicerExpression(<span class="built_in">exp</span>);   <span class="comment">//2</span>
        <span class="built_in">exp</span> = <span class="built_in">exp</span>.accept(compiler.getValidator());  <span class="comment">//3</span>
    }
    <span class="keyword">switch</span> (resultStyle) {
        <span class="keyword">case</span> LIST:
            <span class="keyword">return</span> compiler.compileList(<span class="built_in">exp</span>, <span class="keyword">false</span>);
        <span class="keyword">case</span> MUTABLE_LIST:
            <span class="keyword">return</span> compiler.compileList(<span class="built_in">exp</span>, <span class="keyword">true</span>);
        <span class="keyword">case</span> ITERABLE:
            <span class="keyword">return</span> compiler.compileIter(<span class="built_in">exp</span>);  <span class="comment">//4</span>
        <span class="keyword">default</span>:
            <span class="keyword">throw</span> Util.unexpected(resultStyle);
    }
}
</code></pre><ol>
<li>第1行判断表达式是否是切片轴，如果是，进入2,3步。</li>
<li>标准化表达式，即将表达式封装成集表达式（{}表达式）。</li>
<li>对表达式执行前面1.1的解析过程，将UnresolvedFunCall转换成ResolvedFunCall。</li>
<li>最后使用compiler编译迭代，返回计算器。</li>
</ol>
<p>接下来我们看它是如何编译的。我们看<code>compiler.compileIter(exp)</code>这个方法。</p>
<pre><code><span class="keyword">public</span> IterCalc compileIter(<span class="built_in">Exp</span> <span class="built_in">exp</span>) {
    IterCalc calc =
        (IterCalc) compileAs(<span class="built_in">exp</span>, <span class="literal">null</span>, ResultStyle.ITERABLE_ONLY);
    <span class="keyword">if</span> (calc == <span class="literal">null</span>) {
        calc = (IterCalc) compileAs(<span class="built_in">exp</span>, <span class="literal">null</span>, ResultStyle.ANY_ONLY);
        assert calc != <span class="literal">null</span>;
    }
    <span class="keyword">return</span> calc;
}
</code></pre><p>这个方法比较简答，先用ResultStyle.ITERABLE_ONLY编译一次，如果为空，继续用ResultStyle.ANY_ONLY编译一次。</p>
<p>我们继续看compileAs方法：</p>
<pre><code><span class="keyword">public</span> Calc compileAs(
    Exp <span class="built_in">exp</span>,
    Type resultType,
    List&lt;ResultStyle&gt; preferredResultTypes)
{
    <span class="built_in">int</span> substitutions = <span class="number">0</span>;
    List&lt;ResultStyle&gt; <span class="built_in">save</span> = <span class="keyword">this</span>.resultStyles;
    <span class="keyword">try</span> {
        <span class="keyword">this</span>.resultStyles = preferredResultTypes;
        <span class="keyword">if</span> (resultType != <span class="keyword">null</span> &amp;&amp; resultType != <span class="built_in">exp</span>.getType()) {  <span class="comment">//1</span>
            <span class="keyword">if</span> (resultType <span class="keyword">instanceof</span> MemberType) {
                <span class="keyword">return</span> compileMember(<span class="built_in">exp</span>);                        <span class="comment">//2</span>
            } <span class="keyword">else</span> <span class="keyword">if</span> (resultType <span class="keyword">instanceof</span> LevelType) {
                <span class="keyword">return</span> compileLevel(<span class="built_in">exp</span>);
            } <span class="keyword">else</span> <span class="keyword">if</span> (resultType <span class="keyword">instanceof</span> HierarchyType) {
                <span class="keyword">return</span> compileHierarchy(<span class="built_in">exp</span>);
            } <span class="keyword">else</span> <span class="keyword">if</span> (resultType <span class="keyword">instanceof</span> DimensionType) {
                <span class="keyword">return</span> compileDimension(<span class="built_in">exp</span>);
            } <span class="keyword">else</span> <span class="keyword">if</span> (resultType <span class="keyword">instanceof</span> ScalarType) {
                <span class="keyword">return</span> compileScalar(<span class="built_in">exp</span>, <span class="keyword">false</span>);
            }
        }                                                       <span class="comment">//3</span>
        <span class="keyword">final</span> Calc calc = compile(<span class="built_in">exp</span>);                         <span class="comment">//4</span>
        <span class="keyword">if</span> (substitutions &gt; <span class="number">0</span>) {
            <span class="keyword">final</span> IterCalc iterCalc = (IterCalc) calc;
            <span class="keyword">if</span> (iterCalc == <span class="keyword">null</span>) {
                <span class="keyword">this</span>.resultStyles =
                    Collections.singletonList(ResultStyle.ITERABLE);
                <span class="keyword">return</span> compile(<span class="built_in">exp</span>);
            } <span class="keyword">else</span> {
                <span class="keyword">return</span> iterCalc;
            }
        }
        <span class="keyword">return</span> calc;
    } <span class="keyword">finally</span> {
        <span class="keyword">this</span>.resultStyles = <span class="built_in">save</span>;
    }
}
</code></pre><p>根据结果集类型，使用不同的编译方式。我们看代码块1-3之间的，它的主要作用是因为表达式的类型跟返回的结果集类型不同，所以要特殊对待。例如，我们看第2行，如果表达式的类型的MemberType，但是返回类型是HierarchyType，这时我们就需要将其转换为<hierarchytype>类型了。</hierarchytype></p>
<p>对于返回结果集合表达式类似相同的情况，在第4行中，<code>compile(exp)</code>调用了ResolvedFunCall的<code>exp.accept(this)</code>，最后调用<code>funDef.compileCall(this, compiler)</code>方法，创建了一个计算器（Calc）。注意这里的compileCall是需要在自己在函数中实现的。</p>
<h3 id="二，函数的执行过程。">二，函数的执行过程。</h3><p>函数的执行是在RolapResult结果集中进行的。具体的方法是<code>executeAxis(
        Evaluator evaluator,
        QueryAxis queryAxis,
        Calc axisCalc,
        boolean construct,
        AxisMemberList axisMembers)</code><br>方法。我们可以看到方法中有一段代码</p>
<pre><code><span class="keyword">final</span> TupleIterable iterable =
          ((IterCalc) axisCalc).evaluateIterable(evaluator);
</code></pre><p>为什么要是一个IterCalc呢，留给大家思考。这里通过执行IterCalc的<code>evaluateIterable()</code>方法，来完成整个轴的执行过程。具体的过程是根据轴的不同调用不同的计算器，在计算器里层继续调用参数的计算器（参考各个FunDef的实现），直到返回结果为止。</p>
]]></content>
    <summary type="html">
    <![CDATA[<h2 id="MDX函数的执行过程详解">MDX函数的执行过程详解</h2><p>笔者注：由于将来公司可能会有对函数的二次开发需求，所以为公司写了函数的执行过程，以供将来使用。</p>
<h3 id="一，Query对象的创建">一，Query对象的创建</h3><p>在Query对象中，最主要的是resolve()方法，这里涉及到了2个非常重要的功能。</p>
<ul>
<li><p>第一，轴（查询轴和切片轴）上表达式(Exp)的转换。</p>
<p>  主要是将表达式从UnresolvedFunCall转换为ResolvedFunCall。</p>
</li>
<li><p>第二，轴上的计算器(Calc)的创建。</p>
<p>  主要是针对各个轴如何创建计算器。</p>
</li>
</ul>
<p>下面是的resolve()方法解释：</p>
<pre><code> <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">resolve</span><span class="params">()</span> </span>{
    <span class="keyword">final</span> Validator validator = createValidator(); <span class="comment">//1</span>
    resolve(validator); <span class="comment">// resolve self and children ,2</span>
    <span class="comment">// Create a dummy result so we can use its evaluator</span>
    <span class="keyword">final</span> Evaluator evaluator = Util.createEvaluator(<span class="keyword">this</span>); <span class="comment">//3</span>
    ExpCompiler compiler =
            createCompiler(
                    evaluator, validator, Collections.singletonList(resultStyle)); <span class="comment">//4</span>
    compile(compiler);  <span class="comment">//5</span>
}
</code></pre><ol>
<li>第1行创建校验器(Validator)。</li>
<li>第2行根据Validator转换所有的UnresolvedFunCall到ResolvedFunCall。</li>
<li>第3行针对当前Query创建求值器(Evaluator)。</li>
<li>第4行根据求值器，校验器创建表达式编译器(ExpCompiler)。</li>
<li>第5行根据表达式编译器编译所有的轴，并创建轴上的计算器。</li>
</ol>]]>
    
    </summary>
    
      <category term="mondrian" scheme="http://sqtds.github.io/tags/mondrian/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[mondrian 源码解读（七）-读取member]]></title>
    <link href="http://sqtds.github.io/2014/11/02/2014/mondrian-source-code-7/"/>
    <id>http://sqtds.github.io/2014/11/02/2014/mondrian-source-code-7/</id>
    <published>2014-11-02T10:07:10.000Z</published>
    <updated>2015-05-09T04:12:09.000Z</updated>
    <content type="html"><![CDATA[<p>在前面几节中我们了解了cube的创建和query的创建。有了这2样东西后，我们就可以开始执行mdx语句了。在第二小节中，我们了解了mdx的执行顺序。</p>
<ol>
<li>先执行from，即表示从哪个cube中获取结果。</li>
<li>再执行where，with和axis，即获取member阶段。</li>
<li>有了所有依赖的成员后，最后执行Cell的值。</li>
</ol>
<p>cube的创建已经在前一节中讲过了，接着是member获取阶段，由于这一阶段分为3部分，内容比较多。所以下面我们先将member是如何读取的，后几节再介绍它的执行过程。</p>
<h2 id="MemberSource">MemberSource</h2><p>所有对成员的读取操作都是通过MemberSource来完成的。<br>MemberSource基本功能就是是读取层次的成员操作。<br>我们看下它的基本接口：</p>
<pre><code><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MemberSource</span> </span>{

    <span class="function">RolapHierarchy <span class="title">getHierarchy</span><span class="params">()</span></span>;

    <span class="function"><span class="keyword">boolean</span> <span class="title">setCache</span><span class="params">(MemberCache cache)</span></span>;

    <span class="function">List&lt;RolapMember&gt; <span class="title">getMembers</span><span class="params">()</span></span>;

    <span class="function">List&lt;RolapMember&gt; <span class="title">getRootMembers</span><span class="params">()</span></span>;

    <span class="function"><span class="keyword">void</span> <span class="title">getMemberChildren</span><span class="params">(
        RolapMember parentMember,
        List&lt;RolapMember&gt; children)</span></span>;

    <span class="function"><span class="keyword">void</span> <span class="title">getMemberChildren</span><span class="params">(
        List&lt;RolapMember&gt; parentMembers,
        List&lt;RolapMember&gt; children)</span></span>;

    <span class="function"><span class="keyword">int</span> <span class="title">getMemberCount</span><span class="params">()</span></span>;

    <span class="function">RolapMember <span class="title">lookupMember</span><span class="params">(
        List&lt;Id.Segment&gt; uniqueNameParts,
        <span class="keyword">boolean</span> failIfNotFound)</span></span>;
}    
</code></pre><p>从上面我们可以看到，有3个最基本的接口，分别是获取层次，即当前的MemberSource属于哪个层次，设置缓存对象（为了从缓存中读取member），和获得当前层次的成员。这就是MemberSource的核心功能。</p>
<h2 id="MemberReader">MemberReader</h2><p>我们先看MemberReader的层次图。<br><img src="/img/2014/QQ图片20141102182120.jpg" alt=""><br>从上图中我们看到，MemberReader是继承的MemberSource，所以MemberReader具有MemberSource所有的功能。那么它有哪些功能呢？<br>我们看看它的接口:</p>
<pre><code><span class="class"><span class="keyword">interface</span> <span class="title">MemberReader</span> <span class="keyword">extends</span> <span class="title">MemberSource</span> </span>{

    <span class="function">RolapMember <span class="title">getLeadMember</span><span class="params">(RolapMember member, <span class="keyword">int</span> n)</span></span>;


    <span class="function">List&lt;RolapMember&gt; <span class="title">getMembersInLevel</span><span class="params">(
        RolapLevel level)</span></span>;


    <span class="function"><span class="keyword">void</span> <span class="title">getMemberRange</span><span class="params">(
        RolapLevel level,
        RolapMember startMember,
        RolapMember endMember,
        List&lt;RolapMember&gt; list)</span></span>;

    <span class="function"><span class="keyword">int</span> <span class="title">compare</span><span class="params">(
        RolapMember m1,
        RolapMember m2,
        <span class="keyword">boolean</span> siblingsAreEqual)</span></span>;

    Map&lt;? extends Member, Access&gt; getMemberChildren(
        RolapMember member,
        List&lt;RolapMember&gt; children,
        MemberChildrenConstraint constraint);

    Map&lt;? extends Member, Access&gt; getMemberChildren(
        List&lt;RolapMember&gt; parentMembers,
        List&lt;RolapMember&gt; children,
        MemberChildrenConstraint constraint);

    <span class="function">List&lt;RolapMember&gt; <span class="title">getMembersInLevel</span><span class="params">(
        RolapLevel level,
        TupleConstraint constraint)</span></span>;

    <span class="function"><span class="keyword">int</span> <span class="title">getLevelMemberCount</span><span class="params">(RolapLevel level)</span></span>;

    <span class="function">MemberBuilder <span class="title">getMemberBuilder</span><span class="params">()</span></span>;

    <span class="function">RolapMember <span class="title">getDefaultMember</span><span class="params">()</span></span>;

    <span class="function">RolapMember <span class="title">getMemberParent</span><span class="params">(RolapMember member)</span></span>;

    <span class="function">RolapMember <span class="title">substitute</span><span class="params">(RolapMember member)</span></span>;

    <span class="function">RolapMember <span class="title">desubstitute</span><span class="params">(RolapMember member)</span></span>;

    <span class="function">RolapMember <span class="title">getMemberByKey</span><span class="params">(
        RolapLevel level,
        List&lt;Comparable&gt; keyValues)</span></span>;
}
</code></pre><p>从上面大家发现没，MemberReader更细化功能，它可以获取到Level上的成员，也可以获取成员的父成员或者成员的子成员，并且在获取成员的时候可以添加约束条件。有了这些功能，我们就可以任意的获取成员了，所以MemberReader是成员获取的主接口。</p>
<h2 id="具体实现">具体实现</h2><h3 id="SmartMemberReader">SmartMemberReader</h3><p>SmartMemberReader实现了MemberReader接口，它实现了维度成员及其子成员的缓存，如果有一个成员位于缓存中，则还会有一个其子成员的列表。它同时缓存了level下的成员们。该类主要的成员有：</p>
<ul>
<li>source：MemberReader，用于实际从数据库中读取维度成员值。</li>
<li>mapMemberToChildren：map，实现成员及其子成员的映射， key为RolapMember，value为List<rolapmember>。</rolapmember></li>
<li>mapKeyToMember： map ，实现所有成员的缓存，其中的key为MmberKey</li>
<li>mapLevelToMembers: map，实现级别及其所有成员的映射， key为RolapLevel，value为List<rolapmember>。</rolapmember></li>
</ul>
<h3 id="SqlMemberSource">SqlMemberSource</h3><p>SqlMemberSource是最终更数据库打交道的类，它会针对要查询对Level生成一个sql，该sql最终通过数据库执行，将查询结果返回后拼装成member。需要注意的一点是，如果该member有parentmember，必须要生成其对应的parentmember。生成过程即先递归的在level中查找其上一级level的member是否缓存，如果没有缓存，则将其加入查询列表中，直到上一级为空或缓存了member。找到所有要查询的level后，最后一次性生成sql向数据库查询数据。</p>
<p>SmartMemberReader的source其实为mondrian.rolap.SqlMemberSource类，该类中反过来又存储了SmartMemberReader对象，作为其cache成员属性。成员读取过程：</p>
<ol>
<li>smartMemberReader.getMemberChildren(parentMembers,children,constrain);</li>
<li>最终通过source.getMemberChildren()…，其中反过来会把找到的children赋予mapKeyToMember。</li>
<li>最终除了将结果返回在children输出参数中，同时也对mapMemberToChildren赋值了。</li>
</ol>
]]></content>
    <summary type="html">
    <![CDATA[<p>在前面几节中我们了解了cube的创建和query的创建。有了这2样东西后，我们就可以开始执行mdx语句了。在第二小节中，我们了解了mdx的执行顺序。</p>
<ol>
<li>先执行from，即表示从哪个cube中获取结果。</li>
<li>再执行where，with和a]]>
    </summary>
    
      <category term="mondrian" scheme="http://sqtds.github.io/tags/mondrian/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[mondrian 源码解读（六）-创建CUBE]]></title>
    <link href="http://sqtds.github.io/2014/10/26/2014/mondrian-source-code-6/"/>
    <id>http://sqtds.github.io/2014/10/26/2014/mondrian-source-code-6/</id>
    <published>2014-10-26T10:07:05.000Z</published>
    <updated>2014-10-26T10:44:13.000Z</updated>
    <content type="html"><![CDATA[<p>在第4节中，构建query的时候，有一个非常重要的东西被我忽略了，那就是构建cube <code>Util.lookupCube(statement.getSchemaReader(), cube, true)</code>，下面我们来谈谈cube是如何构建的。</p>
<h2 id="Cube是什么？">Cube是什么？</h2><p>在第2节中，我们已经谈论了cube，即维度和度量的组合。</p>
<h2 id="RolapCube">RolapCube</h2><p>在理解了Cube后，我们就可以理解RolapCube了。根据字面意思，我们也可以理解为关系型联机分析处理cube。我们看看它里面主要是实体对象：</p>
<pre><code><span class="comment">//不解释</span>
<span class="keyword">protected</span> Dimension[] dimensions;

<span class="comment">//</span>
<span class="keyword">private</span> <span class="keyword">final</span> RolapHierarchy measuresHierarchy;

<span class="comment">/**
 * List of calculated members.
 * 定义在schema中的计算成员，注意公式是如何转换的呢？
 */</span>
<span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">List</span>&lt;Formula&gt; calculatedMemberList = <span class="keyword">new</span> ArrayList&lt;Formula&gt;();

<span class="keyword">private</span> RolapStar star; <span class="comment">//关联的星型模型</span>

<span class="comment">//在Evaluator中用于计算当前成员是否为空，如果当前度量成员中有count聚合，即为当前成员，如果没有找到，创建一个虚拟的count聚合为测量的成员。</span>
RolapBaseCubeMeasure factCountMeasure;
</code></pre><p>calculatedMemberList这里谈一下。如何将schema中定义的计算成员转换成已近解析好的Formula？<br>这里其实很简单，通过构造一个特定的mdx：<br><code>with member [Measures].[aa] as &#39;expression1&#39; ,
[Measures].[bb] as &#39;expression2&#39;select from cube</code><br>然后使用parser解析器解析mdx后返回一个query，query里面的计算成员即为此计算成员。</p>
<h2 id="RolapStar是什么？">RolapStar是什么？</h2><p>我们可以将它理解为一个关联了物理表之间的关系的对象。通过它来生成特定的sql，查询需要的数据。<br>具体实体对象大家可以打开看看，这里不做详细的解释了。</p>
<pre><code><span class="keyword">private</span> DataSource dataSource;
<span class="keyword">private</span> <span class="keyword">final</span> Table factTable;
<span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">List</span>&lt;Column&gt; columnList = <span class="keyword">new</span> ArrayList&lt;Column&gt;();

Table对象..星型模型
<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Table</span> </span>{
    <span class="keyword">private</span> <span class="keyword">final</span> RolapStar star;
    <span class="keyword">private</span> <span class="keyword">final</span> MondrianDef.Relation relation;
    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">List</span>&lt;Column&gt; columnList;
    <span class="keyword">private</span> <span class="keyword">final</span> Table <span class="keyword">parent</span>;
    <span class="keyword">private</span> <span class="keyword">List</span>&lt;Table&gt; children;
    <span class="keyword">private</span> <span class="keyword">final</span> Condition joinCondition;
    <span class="keyword">private</span> <span class="keyword">final</span> String alias;
}
</code></pre><h2 id="源码修改">源码修改</h2><p>cube的创建是根据schema中定义的属性配置来创建cube，这部分没有细看，如何创建其实我们并不关系，理解cube的数据结构就行了。</p>
<p>我们在修改mondrian源码时，将cube创建这块全部去掉了，取而代之的使用了自己的cube创建方式。由于我们底层的存储结构不依赖与xml，而是我们自己定义好的实体间的关系，所以我们将我们的实体转换成了cube。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>在第4节中，构建query的时候，有一个非常重要的东西被我忽略了，那就是构建cube <code>Util.lookupCube(statement.getSchemaReader(), cube, true)</code>，下面我们来谈谈cube是如何构建的。</p>
<]]>
    </summary>
    
      <category term="mondrian" scheme="http://sqtds.github.io/tags/mondrian/"/>
    
      <category term="java" scheme="http://sqtds.github.io/categories/java/"/>
    
  </entry>
  
</feed>